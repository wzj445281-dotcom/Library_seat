# 数据库完整性检查结果

## ✅ 数据库状态

### 已存在的表
- ✅ `categories` - 商品分类表（5条数据）
- ✅ `products` - 商品表（4条数据）
- ✅ `orders` - 订单表
- ✅ `order_items` - 订单明细表
- ✅ `user` - 用户表（2条数据）
- ✅ `user_favorite` - 用户收藏表
- ✅ `coupon` - 优惠券表
- ✅ `user_coupon` - 用户优惠券表
- ✅ `feedback` - 反馈表
- ✅ `seat` - 座位表
- ✅ `work_order` - 工单表

### 数据情况
- ✅ **商品数据**：4条（已初始化）
- ✅ **用户数据**：2条（已初始化）
- ✅ **分类数据**：5条（已初始化）

## ⚠️ 需要检查的表

### 1. user_address 表（地址管理功能）

如果这个表不存在，地址管理功能无法使用。

**检查命令：**
```bash
mysql -uroot -p000000 zhizuo -e "SHOW TABLES LIKE 'user_address';"
```

**如果不存在，执行：**
```bash
mysql -uroot -p000000 zhizuo < seat_server/create_user_address_table.sql
```

### 2. orders 表的字段（优惠券功能）

检查订单表是否有以下字段：
- `coupon_id` - 使用的优惠券ID
- `discount_amount` - 优惠金额
- `original_amount` - 订单原价

**如果缺少，执行：**
```bash
mysql -uroot -p000000 zhizuo < seat_server/alter_orders_table.sql
```

## 🔧 快速修复步骤

### 步骤1：检查并创建缺失的表

```bash
# 进入项目目录
cd E:\Coding\Wechat_project\bookSeats\seat_server

# 检查 user_address 表是否存在
mysql -uroot -p000000 zhizuo -e "SHOW TABLES LIKE 'user_address';"

# 如果不存在，创建表
mysql -uroot -p000000 zhizuo < create_user_address_table.sql
```

### 步骤2：检查并更新订单表结构

```bash
# 检查订单表字段
mysql -uroot -p000000 zhizuo -e "DESCRIBE orders;"

# 如果缺少字段，执行更新
mysql -uroot -p000000 zhizuo < alter_orders_table.sql
```

### 步骤3：验证数据

```bash
# 查看商品数据
mysql -uroot -p000000 zhizuo -e "SELECT id, name, price FROM products LIMIT 5;"

# 查看分类数据
mysql -uroot -p000000 zhizuo -e "SELECT * FROM categories;"

# 查看用户数据
mysql -uroot -p000000 zhizuo -e "SELECT id, username, name FROM user;"
```

## 📝 使用 Navicat/DBeaver（推荐）

如果使用图形化工具：

1. **连接到数据库**
   - Host: `localhost`
   - Port: `3306`
   - Username: `root`
   - Password: `000000`
   - Database: `zhizuo`

2. **执行SQL脚本**
   - 打开 `seat_server/create_user_address_table.sql`
   - 执行SQL
   - 打开 `seat_server/alter_orders_table.sql`
   - 执行SQL

3. **验证**
   - 刷新表列表，确认 `user_address` 表存在
   - 查看 `orders` 表结构，确认有优惠券相关字段

## ✅ 完成后的验证

执行完所有SQL后：

1. **重启后端服务**
   ```bash
   cd seat_server
   mvn spring-boot:run
   ```

2. **验证启动**
   - 应该看到 `Started ZhizuoApplication` 而不是数据库错误
   - 访问 http://localhost:8080/doc.html 应该能看到API文档

3. **测试功能**
   - 登录注册功能
   - 商品浏览功能
   - 地址管理功能（如果之前缺少表）
   - 下单功能

## 🎯 总结

数据库**基本完整**，有：
- ✅ 核心表结构
- ✅ 初始化数据（商品、分类、用户）

**可能缺少：**
- ⚠️ `user_address` 表（地址管理功能需要）
- ⚠️ `orders` 表的优惠券字段（优惠券功能需要）

执行上述SQL脚本后，数据库就完全准备好了！

