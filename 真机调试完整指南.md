# 真机调试完整指南

## 🔍 问题现象

- ✅ 真机模拟能看到网络连接
- ❌ 手机演示时点餐等功能不能实现

## ✅ 检查清单

### 1. 确认网络连接

#### 1.1 检查手机和电脑是否在同一局域网
- 手机和电脑必须连接同一个 WiFi
- 不能使用手机热点（因为电脑连接手机热点后，手机无法访问电脑的IP）

#### 1.2 确认电脑的IP地址
```bash
# Windows
ipconfig

# Mac/Linux
ifconfig
```
找到连接WiFi的网卡的 IPv4 地址，例如：`192.168.78.196`

#### 1.3 确认后端服务正在运行
```bash
# 检查8080端口是否被占用
netstat -ano | findstr :8080

# 应该看到：
# TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING
```

#### 1.4 确认防火墙设置
- Windows：允许 8080 端口通过防火墙
- 或者临时关闭防火墙测试

### 2. 配置前端 BASE_URL

#### 2.1 检查当前配置
文件：`seat_client/utils/request.js`

当前配置：
```javascript
const BASE_URL = 'http://192.168.78.196:8080/api';
```

#### 2.2 如果IP地址不对，修改为你的IP
```javascript
// 将 192.168.78.196 替换为你的实际IP地址
const BASE_URL = 'http://你的IP:8080/api';
```

### 3. 微信开发者工具配置

#### 3.1 真机调试设置
1. 打开微信开发者工具
2. 点击右上角"详情"
3. 切换到"本地设置"标签
4. **必须勾选：**
   - ✅ **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**
   - ✅ **不校验安全域名（TLS版本）**

#### 3.2 真机调试步骤
1. 点击工具栏"预览"按钮（或按 Ctrl+P）
2. 用微信扫描二维码
3. 在手机上打开小程序
4. 查看手机上的网络请求是否成功

### 4. 后端配置检查

#### 4.1 CORS 配置
文件：`seat_server/src/main/java/com/example/zhizuo/config/WebMvcConfig.java`

当前配置：
```java
registry.addMapping("/api/**")
    .allowedOriginPatterns("*") // 允许所有来源
    .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
    .allowedHeaders("*")
    .allowCredentials(true)
    .maxAge(3600);
```
✅ 已配置允许所有来源

#### 4.2 Security 配置
文件：`seat_server/src/main/java/com/example/zhizuo/config/SecurityConfig.java`

当前配置：
```java
.antMatchers("/api/**").permitAll() // 放行所有API
```
✅ 已放行所有API

### 5. 测试网络连接

#### 5.1 在手机上测试
1. 打开手机浏览器
2. 访问：`http://你的IP:8080/doc.html`
3. 如果能看到 API 文档，说明网络连接正常

#### 5.2 在微信开发者工具控制台测试
打开调试器 → Console 标签，输入：
```javascript
wx.request({
  url: 'http://192.168.78.196:8080/api/app/product/list',
  method: 'GET',
  success: (res) => {
    console.log('网络连接成功', res);
  },
  fail: (err) => {
    console.error('网络连接失败', err);
  }
});
```

### 6. 常见问题排查

#### 问题1：手机无法连接后端
**可能原因：**
- IP地址不正确
- 防火墙阻止连接
- 手机和电脑不在同一局域网

**解决方法：**
1. 确认IP地址：`ipconfig` 查看实际IP
2. 修改 `request.js` 中的 `BASE_URL`
3. 检查防火墙设置
4. 确保手机和电脑连接同一WiFi

#### 问题2：API请求返回 403 或 CORS 错误
**可能原因：**
- CORS配置不正确
- Security配置阻止了请求

**解决方法：**
1. 检查 `WebMvcConfig.java` 中的 CORS 配置
2. 检查 `SecurityConfig.java` 中的放行规则
3. 重启后端服务

#### 问题3：点餐功能无法使用
**可能原因：**
- API路径不正确
- 登录状态未保存
- 购物车数据未正确存储

**解决方法：**
1. 检查控制台错误信息
2. 确认登录是否成功
3. 检查购物车数据是否正确存储到本地

#### 问题4：真机调试时看不到网络请求
**解决方法：**
1. 在手机上打开"微信开发者工具" → "真机调试"
2. 查看网络请求日志
3. 或者在电脑上查看微信开发者工具的网络面板

### 7. 完整测试流程

#### 步骤1：启动后端
```bash
cd seat_server
mvn spring-boot:run
```

#### 步骤2：确认后端运行
- 浏览器访问：`http://localhost:8080/doc.html`
- 应该能看到 API 文档

#### 步骤3：配置前端
- 确认 `request.js` 中的 `BASE_URL` 是正确的IP地址
- 清除缓存并重新编译

#### 步骤4：真机调试
1. 点击"预览"按钮
2. 用微信扫描二维码
3. 在手机上测试功能

#### 步骤5：查看日志
- 在微信开发者工具中查看控制台
- 在手机上查看网络请求日志
- 在后端查看请求日志

## 🎯 快速检查命令

### Windows
```bash
# 1. 查看IP地址
ipconfig

# 2. 检查8080端口
netstat -ano | findstr :8080

# 3. 测试后端连接（在浏览器中）
http://你的IP:8080/doc.html
```

### Mac/Linux
```bash
# 1. 查看IP地址
ifconfig

# 2. 检查8080端口
netstat -an | grep 8080

# 3. 测试后端连接（在浏览器中）
http://你的IP:8080/doc.html
```

## 📝 配置文件位置

### 前端
- `seat_client/utils/request.js` - API基础URL配置

### 后端
- `seat_server/src/main/java/com/example/zhizuo/config/WebMvcConfig.java` - CORS配置
- `seat_server/src/main/java/com/example/zhizuo/config/SecurityConfig.java` - Security配置

## ⚠️ 重要提示

1. **真机调试必须使用局域网IP**，不能用 `localhost` 或 `127.0.0.1`
2. **手机和电脑必须在同一局域网**（同一WiFi）
3. **微信开发者工具必须勾选"不校验合法域名"**
4. **防火墙可能阻止连接**，需要允许8080端口
5. **每次修改IP地址后，需要清除缓存并重新编译**

## 🎉 成功标志

如果配置正确，你应该能够：
- ✅ 在手机上打开小程序
- ✅ 看到商品列表
- ✅ 能够登录/注册
- ✅ 能够添加商品到购物车
- ✅ 能够提交订单
- ✅ 能够查看订单详情

## 📞 如果还有问题

1. 查看控制台错误信息
2. 查看后端日志
3. 检查网络连接
4. 确认所有配置都正确

