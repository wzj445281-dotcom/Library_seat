# 图片 404 问题修复说明

## 问题分析

图片文件已经存在于 `seat_server/src/main/resources/static/images/product/` 目录中，但是访问时返回 404 错误。

## 已完成的修复

### 1. 静态资源配置优化 ✅

**文件**：`seat_server/src/main/java/com/example/zhizuo/config/WebMvcConfig.java`

已更新静态资源处理器配置，添加了缓存设置：

```java
registry.addResourceHandler("/static/**")
        .addResourceLocations("classpath:/static/", "classpath:/static/images/")
        .setCachePeriod(3600); // 缓存1小时
```

### 2. Security 配置 ✅

**文件**：`seat_server/src/main/java/com/example/zhizuo/config/SecurityConfig.java`

已确认 `/static/**` 路径已放行，无需修改。

## 需要您执行的操作

### 步骤 1：重启后端服务 ⚠️ 重要

**必须重启后端服务**，配置更改才能生效：

```bash
# 停止当前运行的后端服务（Ctrl+C）
# 然后重新启动
cd seat_server
mvn spring-boot:run
```

或者如果使用 IDE：
- 停止当前运行的 Spring Boot 应用
- 重新运行 `ZhizuoApplication.java`

### 步骤 2：验证图片访问

在浏览器中访问以下 URL，确认图片可以正常访问：

```
http://localhost:8080/static/images/product/american.jpg
http://localhost:8080/static/images/product/latte.jpg
http://localhost:8080/static/images/product/cappuccino.jpg
http://localhost:8080/static/images/product/coconut_latte.jpg
```

**预期结果**：应该能看到图片，而不是 404 错误。

### 步骤 3：清除小程序缓存

1. 在微信开发者工具中：
   - 点击"清缓存" → "清除数据缓存"
   - 或者关闭并重新打开项目

2. 重新编译小程序

### 步骤 4：测试小程序

在小程序中查看菜单页面，图片应该能正常显示。

## 如果仍然无法访问

### 方案 A：检查文件路径

确认图片文件确实在以下位置：
```
seat_server/src/main/resources/static/images/product/
├── american.jpg
├── latte.jpg
├── cappuccino.jpg
├── coconut_latte.jpg
├── coconut_cloud.jpg
├── matcha_ice.jpg
└── choco_ice.jpg
```

### 方案 B：检查后端日志

查看后端启动日志，确认：
- 没有关于静态资源加载的错误
- Spring Boot 正常启动

### 方案 C：使用绝对路径测试

在浏览器中直接访问：
```
http://localhost:8080/static/images/product/american.jpg
```

如果浏览器中能访问，但小程序中不能，可能是：
- 小程序网络权限问题
- 需要配置小程序域名白名单（开发环境可以勾选"不校验合法域名"）

### 方案 D：临时使用占位图

如果图片仍然无法访问，可以临时修改代码使用网络图片作为占位：

在 `seat_client/pages/menu/index.js` 中，已经有默认占位图逻辑：
```javascript
} else if (!imageUrl) {
  // 如果没有图片，使用默认占位图
  imageUrl = 'https://images.unsplash.com/photo-1541167760496-1628856ab772?w=200&h=200&fit=crop';
}
```

## 验证清单

- [ ] 后端服务已重启
- [ ] 浏览器可以访问 `http://localhost:8080/static/images/product/american.jpg`
- [ ] 小程序缓存已清除
- [ ] 小程序中图片正常显示

## 常见问题

### Q1: 重启后仍然 404
- 检查图片文件是否真的存在
- 检查文件权限
- 查看后端日志是否有错误信息

### Q2: 浏览器能访问，小程序不能
- 确认微信开发者工具中已勾选"不校验合法域名"
- 检查小程序网络请求是否被拦截

### Q3: 部分图片能显示，部分不能
- 检查文件名是否正确（区分大小写）
- 检查数据库中的路径是否正确

---

**重要提示**：配置更改后，**必须重启后端服务**才能生效！

