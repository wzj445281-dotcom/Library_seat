# 前后端功能测试检查清单

## 1. 后端服务检查

### 1.1 启动后端服务
```bash
cd seat_server
mvn spring-boot:run
# 或
java -jar target/zhizuo-server-1.2.0.jar
```

### 1.2 检查服务状态
- [ ] 后端服务在 `http://localhost:8080` 正常运行
- [ ] 数据库连接正常（MySQL: zhizuo）
- [ ] API 文档可访问（如果配置了 Swagger/Knife4j）

### 1.3 测试基础 API
```bash
# 测试健康检查
curl http://localhost:8080/api/app/health

# 测试菜单接口
curl http://localhost:8080/api/app/store/menu

# 测试演示登录
curl -X POST http://localhost:8080/api/app/auth/mock-login
```

## 2. 前端小程序检查

### 2.1 微信开发者工具配置
- [ ] 项目路径：`seat_client`
- [ ] AppID：已配置（测试号或正式号）
- [ ] 本地设置：勾选"不校验合法域名、web-view、TLS版本以及HTTPS证书"
- [ ] 编译模式：选择"普通编译"

### 2.2 检查基础配置
- [ ] `app.json` 中所有页面路径正确
- [ ] `utils/request.js` 中 `BASE_URL` 为 `http://localhost:8080/api`
- [ ] 所有页面文件存在（.js, .wxml, .wxss, .json）

## 3. 功能测试

### 3.1 登录功能测试
- [ ] **演示模式登录**
  - 点击"🎓 答辩演示模式 (免验证)"按钮
  - 应该成功登录并跳转到首页
  - 检查 `wx.getStorageSync('token')` 和 `wx.getStorageSync('userInfo')` 有值

- [ ] **账号密码注册**
  - 切换到注册模式
  - 输入用户名、昵称、密码
  - 点击"注册"按钮
  - 应该显示"注册成功请登录"
  - 检查数据库中是否有新用户记录

- [ ] **账号密码登录**
  - 使用注册的账号密码登录
  - 应该成功登录并跳转到首页
  - Token 和用户信息应该正确存储

### 3.2 菜单功能测试
- [ ] **菜单加载**
  - 进入菜单页面 (`/pages/menu/index`)
  - 应该显示咖啡商品列表
  - 商品图片应该正常加载

- [ ] **搜索功能**
  - 在搜索框输入关键词（如"拿铁"）
  - 应该显示匹配的商品
  - 搜索结果应该正确

- [ ] **收藏功能**
  - 点击商品的心形图标
  - 应该切换收藏状态（❤️/🤍）
  - 收藏状态应该持久化

- [ ] **分类筛选**
  - 点击不同分类标签
  - 应该显示对应分类的商品

### 3.3 购物车功能测试
- [ ] **添加商品**
  - 点击商品卡片
  - 选择规格和数量
  - 添加到购物车
  - 购物车图标应该显示数量

- [ ] **购物车管理**
  - 打开购物车
  - 修改商品数量
  - 删除商品
  - 清空购物车

### 3.4 下单功能测试
- [ ] **创建订单**
  - 从购物车或商品详情页下单
  - 选择座位（如果有选座功能）
  - 选择优惠券（如果有）
  - 提交订单
  - 应该生成订单并跳转到订单详情页

- [ ] **订单详情**
  - 订单信息应该正确显示
  - 取餐码应该显示（大号文字）
  - 订单状态应该正确
  - 进度条应该正确显示

### 3.5 订单列表测试
- [ ] **订单列表**
  - 进入"我的订单"页面
  - 应该显示所有订单
  - 订单状态应该正确显示
  - 可以点击订单查看详情

### 3.6 我的页面测试
- [ ] **页面加载**
  - 进入"我的"页面 (`/pages/mine/mine`)
  - 应该显示用户信息
  - 头像和昵称应该正确显示

- [ ] **功能入口**
  - "我的订单"应该能正常跳转
  - 其他功能入口应该正常

### 3.7 WebSocket 实时更新测试
- [ ] **连接建立**
  - 打开订单详情页
  - 检查控制台是否有 WebSocket 连接成功日志
  - 连接地址应该是 `ws://localhost:8080/ws/order/{orderId}`

- [ ] **状态更新**
  - 在后端手动更新订单状态（通过 API 或数据库）
  - 前端应该收到 WebSocket 消息
  - 订单状态应该自动更新
  - 页面应该自动刷新

## 4. 错误处理测试

### 4.1 网络错误
- [ ] 后端服务关闭时，前端应该显示友好的错误提示
- [ ] 请求超时应该正确处理

### 4.2 数据验证
- [ ] 注册时用户名重复应该提示错误
- [ ] 登录时密码错误应该提示错误
- [ ] 必填字段为空应该提示错误

### 4.3 页面错误
- [ ] 订单不存在时应该显示友好提示
- [ ] 商品不存在时应该显示友好提示

## 5. 性能测试

### 5.1 加载速度
- [ ] 菜单页面首次加载应该在 2 秒内完成
- [ ] 图片应该使用懒加载
- [ ] 请求应该使用缓存（如果配置了）

### 5.2 内存使用
- [ ] 长时间使用不应该出现内存泄漏
- [ ] 页面切换应该流畅

## 6. 兼容性测试

### 6.1 微信开发者工具
- [ ] 在微信开发者工具中所有功能正常

### 6.2 真机调试
- [ ] 在真机上所有功能正常
- [ ] 网络请求正常（需要配置代理或使用局域网 IP）

## 7. 数据库检查

### 7.1 数据完整性
- [ ] 用户表有测试数据
- [ ] 商品表有咖啡相关数据
- [ ] 分类表有数据
- [ ] 订单表可以正常插入数据

### 7.2 数据一致性
- [ ] 订单和订单项关联正确
- [ ] 用户和订单关联正确
- [ ] 商品和分类关联正确

## 8. 常见问题排查

### 8.1 后端无法启动
- 检查 MySQL 是否运行
- 检查端口 8080 是否被占用
- 检查 `application.yml` 配置是否正确
- 查看日志文件

### 8.2 前端无法连接后端
- 检查 `BASE_URL` 配置
- 检查微信开发者工具的"不校验合法域名"是否勾选
- 检查后端服务是否运行
- 检查防火墙设置

### 8.3 页面报错
- 检查控制台错误信息
- 检查文件路径是否正确
- 检查模块导入路径是否正确
- 检查 JSON 配置文件是否正确

### 8.4 WebSocket 连接失败
- 检查后端 WebSocket 配置
- 检查 `SecurityConfig` 是否允许 `/ws/**` 路径
- 检查前端 WebSocket URL 是否正确
- 检查网络连接

## 9. 测试结果记录

### 测试环境
- 操作系统：Windows 10
- 微信开发者工具版本：_______
- Java 版本：_______
- MySQL 版本：_______
- Spring Boot 版本：2.7.18

### 测试时间
- 开始时间：_______
- 结束时间：_______

### 测试结果
- 通过项：_______
- 失败项：_______
- 待修复问题：_______

