# 登录注册功能完成说明

## 完成时间
2025-01-XX

## 功能概述
实现了基于手机号、姓名、验证码的登录注册功能，支持未登录自动跳转到登录页。

## 完成内容

### 1. ✅ 后端功能完善

#### 1.1 修改 DTO
- **UserLoginDTO**：改为手机号 + 验证码
- **UserRegisterDTO**：改为手机号 + 姓名 + 验证码

#### 1.2 新增接口
- **发送验证码**：`GET /api/auth/send-code?phone=xxx`
  - 生成6位随机验证码
  - 存储到 Redis，5分钟过期
  - 开发模式下在控制台打印验证码

#### 1.3 修改接口
- **登录接口**：`POST /api/auth/login`
  - 验证手机号和验证码
  - 通过手机号查找用户
  - 生成 JWT Token

- **注册接口**：`POST /api/auth/register`
  - 验证手机号和验证码
  - 检查手机号是否已注册
  - 创建新用户，使用手机号作为用户名

### 2. ✅ 前端功能完善

#### 2.1 登录页面改造
- ✅ 支持手机号输入
- ✅ 支持验证码输入和发送
- ✅ 支持登录/注册模式切换
- ✅ 注册模式需要输入姓名
- ✅ 验证码倒计时功能（60秒）
- ✅ 手机号格式验证

#### 2.2 登录状态管理
- ✅ `app.js` 中检查登录状态
- ✅ 主要页面（菜单页）添加登录检查
- ✅ 未登录自动跳转到登录页
- ✅ 登录成功后保存 Token 和用户信息

#### 2.3 网络配置
- ✅ 支持本地开发（localhost）
- ✅ 支持真机调试（需要修改为局域网IP）
- ✅ GET 请求支持 query 参数

## 使用说明

### 1. 真机调试配置

**问题：** 真机调试时无法连接 `localhost`

**解决方法：**

1. **获取本机IP地址**
   - Windows：打开命令提示符，输入 `ipconfig`，找到"IPv4 地址"
   - Mac/Linux：打开终端，输入 `ifconfig`，找到 `inet` 地址

2. **修改前端配置**
   - 打开 `seat_client/utils/request.js`
   - 修改 `BASE_URL` 为你的局域网IP，例如：
     ```javascript
     const BASE_URL = 'http://192.168.1.5:8080/api';
     ```

3. **确保后端服务运行**
   - 后端必须运行在 `http://0.0.0.0:8080` 或 `http://你的IP:8080`
   - 检查防火墙是否允许 8080 端口

### 2. 验证码获取

**开发模式：**
- 验证码会在后端控制台打印
- 格式：`【瑞幸咖啡】验证码：123456，5分钟内有效。请勿泄露给他人。`

**生产模式：**
- 需要接入短信服务商（如阿里云、腾讯云）
- 修改 `AuthController.sendCode()` 方法，调用真实的短信API

### 3. 登录流程

1. 用户打开小程序
2. 系统检查是否有 Token
3. 如果没有 Token，跳转到登录页
4. 用户输入手机号，点击"获取验证码"
5. 输入验证码，点击"登录"
6. 登录成功后，保存 Token 和用户信息
7. 跳转到菜单页

### 4. 注册流程

1. 在登录页点击"没有账号？去注册"
2. 输入手机号、姓名
3. 点击"获取验证码"
4. 输入验证码，点击"注册"
5. 注册成功后，自动切换到登录模式
6. 使用刚才注册的手机号登录

## API 接口说明

### 1. 发送验证码
```
GET /api/auth/send-code?phone=13800138000
```

**响应：**
```json
{
  "code": 200,
  "message": "验证码已发送（开发模式：验证码为 123456）",
  "data": "验证码已发送（开发模式：验证码为 123456）"
}
```

### 2. 登录
```
POST /api/auth/login
Content-Type: application/json

{
  "phone": "13800138000",
  "code": "123456"
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userId": "1",
    "name": "张三",
    "phone": "13800138000"
  }
}
```

### 3. 注册
```
POST /api/auth/register
Content-Type: application/json

{
  "phone": "13800138000",
  "name": "张三",
  "code": "123456"
}
```

**响应：**
```json
{
  "code": 200,
  "message": "注册成功",
  "data": "注册成功"
}
```

## 数据库字段说明

用户表（`users`）需要包含以下字段：
- `id`：用户ID
- `username`：用户名（使用手机号）
- `phone`：手机号
- `name`：姓名
- `password`：密码（加密存储）
- `points`：积分
- `credit_score`：信用分

## 注意事项

1. **验证码有效期**：5分钟
2. **验证码使用后删除**：登录/注册成功后，验证码会被删除
3. **手机号唯一性**：每个手机号只能注册一次
4. **Token 存储**：使用 `wx.setStorageSync` 存储，小程序关闭后仍然有效
5. **登录状态检查**：主要页面（如菜单页）会在 `onShow` 时检查登录状态

## 后续优化建议

1. **短信服务集成**：接入真实的短信服务商
2. **图形验证码**：防止恶意刷验证码
3. **登录过期处理**：Token 过期后自动跳转登录
4. **记住登录状态**：提供"记住我"选项
5. **第三方登录**：支持微信一键登录

## 总结

所有功能已完整实现：
- ✅ 手机号登录
- ✅ 手机号注册
- ✅ 验证码发送和验证
- ✅ 未登录自动跳转
- ✅ 登录状态保持
- ✅ 真机调试支持

现在用户可以正常使用登录注册功能了！

