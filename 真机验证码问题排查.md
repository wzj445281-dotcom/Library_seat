# 真机验证码问题排查指南

## 🔴 问题现象

真机演示时点击"获取验证码"没有反应或提示错误。

## 🔍 可能的原因

### 1. 网络连接问题（最常见）

**原因：** 真机和电脑不在同一局域网，或IP地址不正确

**检查方法：**
1. 确认真机和电脑连接在**同一个WiFi**网络
2. 确认电脑的IP地址是否正确

**获取电脑IP：**
```bash
ipconfig
# 找到 "IPv4 地址"，例如：192.168.1.100
```

**修改前端配置：**
编辑 `seat_client/utils/request.js` 第7行，确保IP地址正确：
```javascript
const BASE_URL = 'http://192.168.1.100:8080/api';  // 使用你的实际IP
```

**测试连接：**
在真机浏览器中访问：`http://你的IP:8080/doc.html`
- 如果能打开API文档，说明网络连接正常
- 如果打不开，说明网络有问题

### 2. Redis未启动

**原因：** 验证码存储在Redis中，如果Redis未启动，验证码无法存储

**检查方法：**
```bash
# Windows
Get-Service | Where-Object {$_.Name -like "*redis*"}

# 或检查端口
netstat -ano | findstr :6379
```

**解决方案：**
1. **如果未安装Redis：**
   - 下载 Redis for Windows
   - 或使用Docker运行Redis：`docker run -d -p 6379:6379 redis:6.2`

2. **如果Redis未启动：**
   ```bash
   # Windows服务
   net start Redis
   # 或
   Start-Service Redis
   ```

3. **临时方案（不推荐）：**
   - 如果无法安装Redis，可以修改后端代码，使用内存存储验证码（重启后丢失）

### 3. 防火墙阻止

**原因：** Windows防火墙可能阻止了8080端口

**解决方案：**
1. 打开"Windows Defender 防火墙"
2. 点击"高级设置"
3. 添加入站规则，允许8080端口

**快速命令：**
```bash
netsh advfirewall firewall add rule name="Allow 8080" dir=in action=allow protocol=TCP localport=8080
```

### 4. 后端服务未启动

**检查方法：**
- 在电脑浏览器访问：http://localhost:8080/doc.html
- 如果打不开，说明后端未启动

**启动后端：**
```bash
cd E:\Coding\Wechat_project\bookSeats\seat_server
mvn spring-boot:run
```

### 5. API路径错误

**检查：**
- 前端调用：`/api/auth/send-code`
- 后端接口：`GET /api/auth/send-code`
- 应该匹配

## 🔧 快速排查步骤

### 步骤1：检查网络连接

1. **确认真机和电脑在同一WiFi**
2. **获取电脑IP地址**
   ```bash
   ipconfig
   ```
3. **在真机浏览器测试**
   - 打开：`http://你的IP:8080/doc.html`
   - 如果能看到API文档，说明网络正常

### 步骤2：检查Redis

1. **检查Redis是否运行**
   ```bash
   netstat -ano | findstr :6379
   ```
2. **如果未运行，启动Redis**
   - 使用Docker：`docker run -d -p 6379:6379 redis:6.2`
   - 或安装Redis for Windows

### 步骤3：检查后端日志

查看后端启动时的日志，确认：
- ✅ Redis连接成功
- ✅ 服务启动在8080端口
- ✅ 没有错误信息

### 步骤4：测试API

在真机浏览器或Postman中测试：
```
GET http://你的IP:8080/api/auth/send-code?phone=13800138000
```

应该返回：
```json
{
  "code": 200,
  "message": "验证码已发送（开发模式：验证码为 123456）",
  "data": "验证码已发送（开发模式：验证码为 123456）"
}
```

## 🚀 临时解决方案（如果Redis无法启动）

如果无法安装或启动Redis，可以临时修改后端代码，使用内存存储验证码：

**修改 `AuthController.java`：**
```java
// 临时使用内存存储（重启后丢失）
private static final Map<String, String> codeCache = new ConcurrentHashMap<>();

@GetMapping("/send-code")
public ApiResponse<String> sendCode(@RequestParam String phone) {
    // ... 验证手机号格式 ...
    
    String code = String.format("%06d", new Random().nextInt(1000000));
    
    // 使用内存存储（临时方案）
    codeCache.put(phone, code);
    
    // 5分钟后自动删除
    new Timer().schedule(new TimerTask() {
        @Override
        public void run() {
            codeCache.remove(phone);
        }
    }, 5 * 60 * 1000);
    
    System.out.println("【瑞幸咖啡】验证码：" + code);
    return ApiResponse.success("验证码已发送（开发模式：验证码为 " + code + "）");
}
```

**注意：** 这只是临时方案，生产环境必须使用Redis。

## ✅ 验证修复

修复后，在真机上：
1. 打开小程序
2. 输入手机号
3. 点击"获取验证码"
4. 应该看到"验证码已发送"提示
5. 查看后端控制台，应该能看到验证码输出

## 📝 常见错误信息

### "网络连接失败"
- 检查IP地址是否正确
- 检查真机和电脑是否在同一WiFi
- 检查防火墙设置

### "网络服务异常"
- 检查后端是否启动
- 检查Redis是否运行
- 查看后端日志中的错误信息

### 没有任何反应
- 检查前端控制台（微信开发者工具）
- 检查网络请求是否发出
- 检查BASE_URL配置

## 🎯 推荐配置

### 开发环境推荐
1. **使用固定IP**（避免IP变化）
   - 在路由器中为电脑设置静态IP
   - 或使用 `ipconfig /all` 查看MAC地址，在路由器中绑定IP

2. **使用Docker运行Redis**（最简单）
   ```bash
   docker run -d -p 6379:6379 --name redis redis:6.2
   ```

3. **关闭防火墙**（仅开发环境）
   - 开发时可以临时关闭Windows防火墙
   - 生产环境必须配置防火墙规则

## 💡 调试技巧

### 1. 查看前端网络请求

在微信开发者工具中：
1. 打开"调试器"
2. 切换到"Network"标签
3. 点击"获取验证码"
4. 查看请求详情：
   - URL是否正确
   - 状态码是什么
   - 响应内容是什么

### 2. 查看后端日志

在后端启动窗口查看：
- 是否有请求到达
- 是否有错误信息
- Redis连接是否成功

### 3. 使用Postman测试

在Postman中测试API：
```
GET http://192.168.78.196:8080/api/auth/send-code?phone=13800138000
```

如果Postman能成功，说明后端正常，问题在前端配置。

