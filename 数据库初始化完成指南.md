# 数据库初始化完成指南

## ✅ 当前数据库状态

根据检查，你的数据库 `zhizuo` **基本完整**：

### 已存在的表和数据
- ✅ `categories` - 商品分类表（5条数据）
- ✅ `products` - 商品表（4条数据）
- ✅ `orders` - 订单表
- ✅ `order_items` - 订单明细表
- ✅ `user` - 用户表（2条数据）
- ✅ `user_favorite` - 用户收藏表
- ✅ `coupon` - 优惠券表
- ✅ `user_coupon` - 用户优惠券表

### 可能缺失的表
- ⚠️ `user_address` - 用户地址表（地址管理功能需要）

## 🔧 快速修复方法

### 方法1：使用 Navicat/DBeaver（推荐，最简单）

1. **打开数据库工具**（Navicat、DBeaver 或 MySQL Workbench）

2. **连接到数据库**
   - Host: `localhost`
   - Port: `3306`
   - Username: `root`
   - Password: `000000`
   - Database: `zhizuo`

3. **执行SQL脚本**
   
   打开并执行以下文件（在 `seat_server/` 目录下）：
   - `create_user_address_table.sql` - 创建地址表
   - `alter_orders_table.sql` - 更新订单表（如果需要）

### 方法2：使用 MySQL 命令行（如果方法1不行）

1. **打开 MySQL 命令行**
   ```bash
   mysql -uroot -p000000 zhizuo
   ```

2. **执行SQL文件内容**
   
   复制 `seat_server/create_user_address_table.sql` 文件的内容，粘贴到 MySQL 命令行执行。

3. **或者直接执行文件**
   ```bash
   source E:/Coding/Wechat_project/bookSeats/seat_server/create_user_address_table.sql
   ```

### 方法3：手动创建表（最简单）

在数据库工具中执行以下SQL：

```sql
-- 创建用户地址表
CREATE TABLE IF NOT EXISTS `user_address` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL COMMENT '用户ID',
  `name` varchar(50) NOT NULL COMMENT '收货人姓名',
  `phone` varchar(20) NOT NULL COMMENT '收货人电话',
  `province` varchar(50) DEFAULT NULL COMMENT '省份',
  `city` varchar(50) DEFAULT NULL COMMENT '城市',
  `district` varchar(50) DEFAULT NULL COMMENT '区县',
  `detail` varchar(255) NOT NULL COMMENT '详细地址',
  `is_default` tinyint(1) DEFAULT 0 COMMENT '是否默认地址 0:否 1:是',
  `latitude` decimal(10,7) DEFAULT NULL COMMENT '纬度',
  `longitude` decimal(10,7) DEFAULT NULL COMMENT '经度',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_is_default` (`user_id`, `is_default`)
) ENGINE=InnoDB COMMENT='用户收货地址表';
```

## ✅ 验证

执行完SQL后，验证表是否创建成功：

```sql
-- 查看所有表
SHOW TABLES;

-- 查看 user_address 表结构
DESCRIBE user_address;
```

## 🚀 重启后端

数据库初始化完成后：

1. **重启后端服务**
   ```bash
   cd E:\Coding\Wechat_project\bookSeats\seat_server
   mvn spring-boot:run
   ```

2. **验证启动**
   - 应该看到 `Started ZhizuoApplication` 
   - 访问 http://localhost:8080/doc.html 应该能看到API文档

## 📝 总结

你的数据库**已经有数据了**：
- ✅ 4个商品
- ✅ 5个分类
- ✅ 2个用户

**只需要确保 `user_address` 表存在**，地址管理功能就能正常使用了。

如果后端启动时还有其他错误，请把错误信息发给我，我会继续帮你解决！

