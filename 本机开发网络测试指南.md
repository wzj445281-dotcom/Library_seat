# 本机开发网络测试指南

## ✅ 当前配置状态

### 1. HTTP 请求配置
**文件：** `seat_client/utils/request.js`

```javascript
const BASE_URL = 'http://localhost:8080/api';  // ✅ 本机开发配置
```

**说明：**
- ✅ 已配置为 `localhost:8080`，适合本机开发
- ✅ 微信开发者工具会自动代理 `localhost` 请求

### 2. WebSocket 配置
**文件：** `seat_client/utils/websocket.js`

```javascript
// 自动从 request.js 获取 BASE_URL
// 自动转换为: ws://localhost:8080
```

**说明：**
- ✅ 已自动从 `BASE_URL` 转换
- ✅ WebSocket 地址：`ws://localhost:8080/ws/order/{orderNo}`

## 🔧 快速测试步骤

### 步骤1：确认后端服务运行

**检查命令：**
```bash
netstat -ano | findstr :8080
```

**应该看到：**
```
TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING
```

**如果后端未运行，启动后端：**
```bash
cd seat_server
mvn spring-boot:run
```

### 步骤2：配置微信开发者工具（重要！）

1. **打开微信开发者工具**
2. **点击右上角"详情"按钮**
3. **切换到"本地设置"标签**
4. **必须勾选：**
   - ✅ **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**
   - ✅ **不校验安全域名（TLS版本）**

**⚠️ 如果不勾选，所有网络请求都会被拦截！**

### 步骤3：清除缓存并重新编译

1. **点击"编译"旁边的下拉箭头**
2. **选择"清除缓存"**
3. **重新编译**

### 步骤4：测试网络连接

#### 方法1：在浏览器中测试后端

**打开浏览器，访问：**
```
http://localhost:8080/doc.html
```

**结果判断：**
- ✅ **能打开** → 后端正常，可以继续测试小程序
- ❌ **打不开** → 后端未启动，需要先启动后端

#### 方法2：在微信开发者工具控制台测试

1. **打开调试器**（点击"调试器"按钮）
2. **切换到 Console 标签**
3. **输入以下代码：**

```javascript
wx.request({
  url: 'http://localhost:8080/api/auth/send-code?phone=13800138000',
  method: 'GET',
  success: (res) => {
    console.log('✅ 网络连接成功！', res);
  },
  fail: (err) => {
    console.error('❌ 网络连接失败', err);
  }
});
```

**结果判断：**
- ✅ **看到返回数据** → 网络连接正常
- ❌ **看到错误信息** → 检查后端服务和微信开发者工具配置

#### 方法3：在小程序中测试

1. **打开登录页面**
2. **输入手机号**
3. **点击"发送验证码"**
4. **查看是否能成功发送**

**如果成功：**
- ✅ 控制台会显示网络请求成功
- ✅ 会收到验证码（或提示）

**如果失败：**
- ❌ 控制台会显示错误信息
- ❌ 页面会提示"网络连接失败"

## 🎯 网络功能清单

### ✅ 已配置的网络功能

1. **HTTP 请求**
   - ✅ 登录/注册
   - ✅ 商品列表
   - ✅ 订单管理
   - ✅ 地址管理
   - ✅ AI 助手
   - ✅ 优惠券

2. **WebSocket 连接**
   - ✅ 订单状态实时推送
   - ✅ 自动重连机制

### 📝 网络地址说明

**当前配置（本机开发）：**
- HTTP API: `http://localhost:8080/api`
- WebSocket: `ws://localhost:8080/ws/order/{orderNo}`

**如果切换到真机调试：**
需要修改 `seat_client/utils/request.js` 中的 `BASE_URL` 为局域网IP：
```javascript
const BASE_URL = 'http://192.168.1.100:8080/api';  // 替换为你的局域网IP
```

WebSocket 会自动适配，无需手动修改！

## 🐛 常见问题

### 问题1：网络连接失败

**可能原因：**
1. 后端服务未启动
2. 微信开发者工具未配置"不校验合法域名"
3. 端口被占用

**解决方法：**
1. 检查后端服务：`netstat -ano | findstr :8080`
2. 确认微信开发者工具配置
3. 清除缓存并重新编译

### 问题2：WebSocket 连接失败

**可能原因：**
1. 后端服务未启动
2. WebSocket 路径配置错误

**解决方法：**
1. 检查后端服务是否运行
2. 查看控制台日志，确认 WebSocket 连接地址
3. 确认后端 WebSocket 端点：`/ws/order/{orderNo}`

### 问题3：请求返回 404

**可能原因：**
1. API 路径错误
2. 后端路由未配置

**解决方法：**
1. 检查 API 路径是否正确
2. 查看后端日志，确认请求是否到达
3. 访问 `http://localhost:8080/doc.html` 查看 API 文档

## ✅ 验证清单

完成以下检查，确保网络功能正常：

- [ ] 后端服务正在运行（8080端口）
- [ ] 微信开发者工具已配置"不校验合法域名"
- [ ] 已清除缓存并重新编译
- [ ] 浏览器能访问 `http://localhost:8080/doc.html`
- [ ] 控制台测试网络请求成功
- 小程序登录/注册功能正常
- 商品列表能正常加载
- 订单详情页 WebSocket 能连接

## 🎉 总结

**当前状态：**
- ✅ HTTP 请求已配置：`http://localhost:8080/api`
- ✅ WebSocket 已配置：`ws://localhost:8080`（自动转换）
- ✅ 所有网络功能已就绪

**下一步：**
1. 确保后端服务运行
2. 配置微信开发者工具
3. 测试网络连接

**如果一切正常，你现在应该有网络了！** 🎊


