# 项目配置说明

## 📋 问题修复说明

已修复以下配置问题，使前后端项目可以正常运行：

### ✅ 已修复的问题

1. **前端配置问题**
   - 修复了 `seat_client/config/config.js` 和 `seat_client/utils/request.js` 中的端口号（从8080改为8081，与Docker映射一致）
   - 添加了详细的配置说明，提醒用户微信小程序无法使用localhost

2. **后端配置问题**
   - 修复了 `application.yml` 中的 AI API Key 配置（之前错误地填写了URL）
   - 添加了环境变量支持，支持通过环境变量覆盖配置
   - 统一了数据库名称配置

3. **Docker配置问题**
   - 修复了服务名称不一致的问题（统一使用服务名而非容器名）
   - 添加了 AI_DEEPSEEK_KEY 环境变量支持

---

## 🚀 快速启动指南

### 方式一：使用 Docker Compose（推荐）

1. **配置 AI API Key（可选）**
   
   编辑 `docker-compose.yml`，找到 `ai-service` 部分，填入你的 DeepSeek API Key：
   ```yaml
   ai-service:
     environment:
       - DEEPSEEK_API_KEY=sk-your-actual-key-here  # 替换为你的真实Key
   ```
   
   如果不配置，AI功能将使用Mock模式（演示模式）。

2. **启动所有服务**
   ```bash
   docker-compose up --build -d
   ```

3. **查看服务状态**
   ```bash
   docker-compose ps
   ```

4. **查看日志**
   ```bash
   # 查看所有服务日志
   docker-compose logs -f
   
   # 查看特定服务日志
   docker-compose logs -f seat-server
   ```

### 方式二：本地开发（不使用Docker）

#### 后端启动步骤

1. **安装依赖服务**
   - MySQL 8.0（端口3306）
   - Redis（端口6379）
   - RabbitMQ（端口5672）

2. **创建数据库**
   ```sql
   CREATE DATABASE zhizuo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

3. **导入数据库脚本**
   - 执行 `seat_server/init.sql` 或 `seat_server/init_v2.sql`

4. **配置数据库连接**
   
   编辑 `seat_server/src/main/resources/application.yml`：
   ```yaml
   datasource:
     url: jdbc:mysql://localhost:3306/zhizuo?...
     username: root
     password: 000000  # 改为你的MySQL密码
   ```

5. **配置 AI API Key（可选）**
   
   编辑 `seat_server/src/main/resources/application.yml`：
   ```yaml
   ai:
     deepseek:
       key: sk-your-actual-key-here  # 替换为你的真实Key
   ```

6. **启动后端**
   ```bash
   cd seat_server
   mvn spring-boot:run
   ```
   
   后端将在 `http://localhost:8080` 启动

#### 前端启动步骤

1. **配置后端地址**
   
   编辑 `seat_client/config/config.js`：
   ```javascript
   // 如果使用本地后端
   const BASE_URL = 'http://localhost:8080/api';
   
   // 如果后端在Docker中（端口8081）
   const BASE_URL = 'http://localhost:8081/api';
   
   // 真机调试必须使用局域网IP
   const BASE_URL = 'http://192.168.1.5:8081/api';  // 替换为你的实际IP
   ```

2. **在微信开发者工具中打开项目**
   - 打开微信开发者工具
   - 选择"导入项目"
   - 选择 `seat_client` 目录
   - 填写你的AppID（测试号也可以）

3. **重要提示**
   - ⚠️ 微信小程序**无法直接访问localhost**
   - 真机调试时，必须使用局域网IP地址
   - 获取本机IP：Windows使用 `ipconfig`，Mac/Linux使用 `ifconfig`

---

## 🔧 配置详解

### 前端配置

**文件位置：** `seat_client/config/config.js` 和 `seat_client/utils/request.js`

**配置项：**
- `BASE_URL`: 后端API地址
  - 本地开发：`http://localhost:8080/api` 或 `http://localhost:8081/api`
  - 真机调试：`http://你的局域网IP:8081/api`
  - 生产环境：`https://你的域名/api`

### 后端配置

**文件位置：** `seat_server/src/main/resources/application.yml`

**主要配置项：**

1. **数据库配置**
   ```yaml
   datasource:
     url: jdbc:mysql://localhost:3306/zhizuo?...
     username: root
     password: 000000
   ```

2. **Redis配置**
   ```yaml
   redis:
     host: localhost
     port: 6379
   ```

3. **RabbitMQ配置**
   ```yaml
   rabbitmq:
     host: localhost
     port: 5672
     username: guest
     password: guest
   ```

4. **AI配置**
   ```yaml
   ai:
     deepseek:
       key: sk-your-key-here  # DeepSeek API Key
       url: https://api.deepseek.com/chat/completions
       model: deepseek-chat
     service:
       url: http://localhost:5000  # Python AI服务地址
   ```

### Docker配置

**文件位置：** `docker-compose.yml`

**端口映射：**
- 后端服务：`8081:8080`（访问时使用8081）
- MySQL：`3307:3306`（访问时使用3307）
- Redis：`6380:6379`（访问时使用6380）
- RabbitMQ：`5673:5672`（应用端口），`15673:15672`（管理界面）
- AI服务：`5001:5000`（访问时使用5001）

**环境变量：**
- `DEEPSEEK_API_KEY`: DeepSeek API Key（在ai-service中配置）
- `SPRING_DATASOURCE_URL`: 数据库连接URL（在seat-server中自动配置）
- `SPRING_REDIS_HOST`: Redis主机（在seat-server中自动配置）
- `SPRING_RABBITMQ_HOST`: RabbitMQ主机（在seat-server中自动配置）

---

## 🐛 常见问题排查

### 1. 前端无法连接后端

**症状：** 小程序中显示"网络连接失败"

**解决方案：**
- 检查后端是否正常运行（访问 `http://localhost:8081/api` 或查看Docker日志）
- 确认 `config.js` 中的 `BASE_URL` 配置正确
- 真机调试时，确保使用局域网IP而非localhost
- 检查防火墙是否阻止了端口访问

### 2. 后端启动失败

**症状：** 后端启动时报错，无法连接数据库/Redis/RabbitMQ

**解决方案：**
- 检查相关服务是否已启动
- 确认 `application.yml` 中的连接信息正确
- 检查端口是否被占用
- 查看详细错误日志：`docker-compose logs seat-server`

### 3. AI功能不工作

**症状：** AI对话返回Mock数据或报错

**解决方案：**
- 检查是否配置了有效的 DeepSeek API Key
- 确认API Key格式正确（以 `sk-` 开头）
- 检查网络连接（需要访问DeepSeek API）
- 查看AI服务日志：`docker-compose logs ai-service`

### 4. 数据库连接失败

**症状：** 后端启动时提示无法连接数据库

**解决方案：**
- 确认MySQL服务已启动
- 检查数据库名称是否正确（本地：`zhizuo`，Docker：`zhizuo_db`）
- 确认用户名密码正确
- 检查端口是否正确（本地：3306，Docker：3307）

---

## 📞 获取帮助

如果遇到其他问题，请检查：
1. Docker容器日志：`docker-compose logs -f`
2. 后端日志：查看控制台输出或日志文件
3. 前端控制台：在微信开发者工具中查看Console输出

---

## ✅ 验证配置

启动后，可以通过以下方式验证配置是否正确：

1. **后端健康检查**
   ```bash
   curl http://localhost:8081/api/app/ai/chat
   ```

2. **查看Docker服务状态**
   ```bash
   docker-compose ps
   ```
   所有服务应该显示为 "Up"

3. **访问管理界面**
   - 后台管理：http://localhost:8081/admin.html
   - RabbitMQ管理：http://localhost:15673（guest/guest）

---

**最后更新：** 2025-01-XX

