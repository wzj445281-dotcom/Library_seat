# 真机调试快速修复指南

## 🔍 当前问题

- ✅ 真机模拟能看到网络连接
- ❌ 手机演示时点餐等功能不能实现

## ✅ 快速检查步骤

### 1. 确认IP地址配置

#### 1.1 查看你的实际IP地址
```bash
# Windows
ipconfig

# 找到连接WiFi的网卡的 IPv4 地址
# 例如：192.168.48.1 或 192.168.211.1
```

#### 1.2 修改前端配置
文件：`seat_client/utils/request.js`

**当前配置：**
```javascript
const BASE_URL = 'http://192.168.78.196:8080/api';
```

**需要修改为你的实际IP：**
```javascript
// 例如你的IP是 192.168.48.1
const BASE_URL = 'http://192.168.48.1:8080/api';
```

### 2. 确认后端服务运行

```bash
# 检查8080端口
netstat -ano | findstr :8080

# 应该看到：
# TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING
```

### 3. 确认手机和电脑在同一局域网

- ✅ 手机和电脑必须连接**同一个WiFi**
- ❌ 不能使用手机热点（手机无法访问电脑IP）

### 4. 微信开发者工具配置

1. 打开微信开发者工具
2. 点击右上角"详情"
3. 切换到"本地设置"标签
4. **必须勾选：**
   - ✅ **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**

### 5. 测试网络连接

#### 在手机上测试
1. 打开手机浏览器
2. 访问：`http://你的IP:8080/doc.html`
3. 如果能看到 API 文档，说明网络连接正常

#### 在微信小程序中测试
1. 点击"预览"按钮
2. 用微信扫描二维码
3. 在手机上打开小程序
4. 查看是否能加载商品列表

## 🔧 常见问题

### 问题1：手机无法连接后端

**解决方法：**
1. 确认IP地址正确（使用 `ipconfig` 查看）
2. 修改 `request.js` 中的 `BASE_URL`
3. 清除缓存并重新编译
4. 确保手机和电脑在同一WiFi

### 问题2：点餐功能无法使用

**可能原因：**
- API路径不正确
- 登录状态未保存
- 购物车数据未正确存储

**解决方法：**
1. 打开微信开发者工具控制台
2. 查看网络请求日志
3. 检查是否有错误信息
4. 确认API路径是否正确

### 问题3：商品列表加载失败

**检查API路径：**
- 前端调用：`/app/store/menu`
- 后端接口：`/api/app/store/menu`
- ✅ 路径正确

**解决方法：**
1. 检查后端服务是否运行
2. 检查网络连接
3. 查看控制台错误信息

## 📝 完整测试流程

### 步骤1：启动后端
```bash
cd seat_server
mvn spring-boot:run
```

### 步骤2：确认后端运行
- 浏览器访问：`http://localhost:8080/doc.html`
- 应该能看到 API 文档

### 步骤3：配置前端IP
- 修改 `seat_client/utils/request.js` 中的 `BASE_URL`
- 使用你的实际IP地址

### 步骤4：清除缓存
- 在微信开发者工具中清除缓存
- 重新编译

### 步骤5：真机调试
1. 点击"预览"按钮
2. 用微信扫描二维码
3. 在手机上测试功能

## 🎯 快速验证

### 在手机上测试API
1. 打开手机浏览器
2. 访问：`http://你的IP:8080/api/app/store/menu`
3. 应该能看到JSON格式的商品数据

### 在微信小程序中测试
1. 打开小程序
2. 进入"点餐"页面
3. 应该能看到商品列表

## ⚠️ 重要提示

1. **真机调试必须使用局域网IP**，不能用 `localhost` 或 `127.0.0.1`
2. **手机和电脑必须在同一局域网**（同一WiFi）
3. **每次修改IP地址后，需要清除缓存并重新编译**
4. **防火墙可能阻止连接**，需要允许8080端口

## 📞 如果还有问题

1. 查看控制台错误信息
2. 查看后端日志
3. 检查网络连接
4. 确认所有配置都正确
