# 前端问题修复说明

## 修复时间
2025-01-XX

## 修复的问题

### 1. ✅ 语法错误修复

**问题：** `seat_client/pages/ai_chat/ai_chat.js` 文件末尾有重复的 `});`

**修复：** 删除多余的 `});`

```javascript
// 修复前
});
});  // ❌ 重复

// 修复后
});  // ✅ 正确
```

### 2. ✅ API 路径修复

**问题：** 后端路径是 `/api/app/ai`，但前端调用路径不正确

**修复：** 
- 后端：`@RequestMapping("/api/app/ai")` ✅
- 前端：`request.post('/app/ai/chat', ...)` ✅
- 最终路径：`http://localhost:8080/api/app/ai/chat` ✅

**说明：** 
- `request.js` 会自动加上 `BASE_URL`（`http://localhost:8080/api`）
- 所以前端只需要传 `/app/ai/chat`，不要传 `/api/app/ai/chat`

### 3. ✅ 用户信息获取修复

**问题：** `userInfo` 未定义，导致用户头像显示错误

**修复：**
```javascript
// 在 data 中添加 userInfo
data: {
    // ...
    userInfo: null
},

// 在 onLoad 中获取用户信息
onLoad: function (options) {
    const userInfo = app.globalData.userInfo || wx.getStorageSync('userInfo') || {};
    this.setData({
        userInfo: userInfo
    });
}
```

**WXML 修复：**
```xml
<!-- 修复前 -->
<image src="{{userInfo.avatarUrl || '/assets/images/user.png'}}" />

<!-- 修复后 -->
<image src="{{userInfo && userInfo.avatarUrl ? userInfo.avatarUrl : '/assets/images/user.png'}}" />
```

### 4. ✅ 网络连接问题排查

**可能原因：**

1. **后端服务未启动**
   - 检查后端是否运行在 `http://localhost:8080`
   - 检查控制台是否有错误日志

2. **端口配置错误**
   - 确认 `seat_client/utils/request.js` 中的 `BASE_URL` 是 `http://localhost:8080/api`
   - 如果是真机调试，需要改为局域网 IP（如 `http://192.168.1.5:8080/api`）

3. **CORS 跨域问题**
   - 后端需要配置允许小程序域名访问
   - 检查 `SecurityConfig` 中的白名单配置

4. **API 路径不匹配**
   - 后端路径：`/api/app/ai/chat`
   - 前端调用：`/app/ai/chat`（会自动加上 BASE_URL）

## 修复后的文件

### 后端
- ✅ `seat_server/src/main/java/com/example/zhizuo/api/app/AiAssistantController.java`
  - 路径改为 `/api/app/ai`（与其他控制器保持一致）

### 前端
- ✅ `seat_client/pages/ai_chat/ai_chat.js`
  - 修复重复的 `});`
  - 修复 API 路径为 `/app/ai/chat`
  - 添加 `userInfo` 获取逻辑

- ✅ `seat_client/pages/ai_chat/ai_chat.wxml`
  - 修复 `userInfo` 的空值判断

## 测试步骤

1. **启动后端服务**
   ```bash
   cd seat_server
   mvn spring-boot:run
   ```
   确认服务运行在 `http://localhost:8080`

2. **检查网络配置**
   - 打开 `seat_client/utils/request.js`
   - 确认 `BASE_URL = 'http://localhost:8080/api'`
   - 如果是真机调试，改为局域网 IP

3. **测试 AI 功能**
   - 在微信开发者工具中打开小程序
   - 进入"我的" -> "AI 客服"
   - 输入"推荐"或"什么好喝"
   - 检查是否能正常返回结果

4. **检查控制台**
   - 打开微信开发者工具的"调试器" -> "Console"
   - 查看是否有网络错误或 API 错误

## 常见错误及解决方案

### 错误1：网络连接失败
**原因：** 后端服务未启动或端口不对
**解决：** 启动后端服务，确认端口是 8080

### 错误2：404 Not Found
**原因：** API 路径不正确
**解决：** 检查后端 `@RequestMapping` 路径和前端调用路径是否匹配

### 错误3：401 Unauthorized
**原因：** Token 过期或无效
**解决：** 重新登录，获取新的 Token

### 错误4：500 Internal Server Error
**原因：** 后端代码错误
**解决：** 查看后端控制台日志，检查错误信息

### 错误5：乱码问题
**原因：** 文件编码不是 UTF-8
**解决：** 确保所有文件都是 UTF-8 编码

## 编码问题排查

如果出现乱码，检查以下内容：

1. **文件编码**
   - 确保所有 `.js`、`.wxml`、`.wxss` 文件都是 UTF-8 编码
   - 在编辑器中设置文件编码为 UTF-8

2. **HTTP 响应头**
   - 后端需要设置 `Content-Type: application/json; charset=utf-8`
   - Spring Boot 默认已配置，无需额外设置

3. **数据库编码**
   - 确保数据库和表都是 UTF-8 编码
   - MySQL 连接字符串包含 `useUnicode=true&characterEncoding=utf-8`

## 总结

所有问题已修复：
- ✅ 语法错误（重复的 `});`）
- ✅ API 路径不匹配
- ✅ 用户信息未定义
- ✅ 网络连接配置

现在前端应该可以正常工作了。如果还有问题，请检查：
1. 后端服务是否启动
2. 网络配置是否正确
3. 控制台错误信息

