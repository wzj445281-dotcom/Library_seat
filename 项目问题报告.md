# 项目问题报告

## 🔴 严重问题（必须修复）

### 1. 编译错误

**问题：** `ServiceSlot.java` 缺少包声明
- **文件：** `seat_server/src/main/java/com/example/zhizuo/core/entity/ServiceSlot.java`
- **错误：** `The declared package "" does not match the expected package "com.example.zhizuo.core.entity"`
- **状态：** ✅ 已修复

---

### 2. 核心功能未实现

**问题：** `ReservationServiceImpl.reserve()` 方法只有注释，没有实际实现
- **文件：** `seat_server/src/main/java/com/example/zhizuo/core/impl/ReservationServiceImpl.java`
- **影响：** 预约功能无法正常工作
- **代码位置：** 第49-68行
- **状态：** ❌ 待修复

**问题代码：**
```java
@Override
@Transactional(rollbackFor = Exception.class)
public void reserve(Long userId, Long seatId, LocalDateTime startTime, LocalDateTime endTime) {
    // ... 这里是你原有的 Redisson 锁、库存检查、死信队列发送等核心代码 ...
    // 请确保这部分保留你原本的高并发处理逻辑
    // 下面仅为示意：
    /*
    RLock lock = redissonClient.getLock("seat_lock:" + seatId);
    ...
    */
}
```

**建议：** 需要实现完整的预约逻辑，包括：
- Redisson分布式锁
- 座位冲突检查
- 库存/座位可用性验证
- 保存预约记录
- 发送RabbitMQ延迟消息（如果启用）

---

### 3. 数据库字段名不匹配

**问题：** `ReservationServiceImpl` 中查询用户时使用了错误的字段名
- **文件：** `seat_server/src/main/java/com/example/zhizuo/core/impl/ReservationServiceImpl.java`
- **错误代码：** 第33行 `query.eq("student_id", studentId);`
- **问题：** User表使用的是 `username` 字段，不是 `student_id`
- **状态：** ❌ 待修复

**修复建议：**
```java
// 错误：
query.eq("student_id", studentId);

// 正确：
query.eq("username", studentId);
```

---

## ⚠️ 警告问题（建议修复）

### 4. 类型安全问题

**问题：** 多处使用了未参数化的 `Map` 类型
- **影响：** 编译警告，可能导致运行时类型转换错误
- **位置：**
  - `AiAssistantController.java` 第112、115、117、118、156行
  - `AiPredictionService.java` 第73行
  - `AppSeatController.java` 第46行
  - `StoreController.java` 第58行

**修复建议：** 使用泛型 `Map<String, Object>` 替代 `Map`

---

### 5. 未使用的导入和字段

**问题：** 代码中有未使用的导入和字段
- **位置：**
  - `OrderSettlementService.java` - 未使用的导入 `LocalDateTime`
  - `OrderSettlementService.java` - 未使用的字段（originalPrice, discountAmount等）
  - `AiAssistantController.java` - 未使用的字段 `orderService`
  - `AdminProductController.java` - 未使用的导入 `List`

**影响：** 代码冗余，影响可读性
**状态：** ⚠️ 建议清理

---

### 6. 未完成的实现

**问题：** `ReservationServiceImpl.getUserReservations()` 可能有问题
- **文件：** `seat_server/src/main/java/com/example/zhizuo/core/impl/ReservationServiceImpl.java`
- **问题：** 第74行查询条件 `eq("student_id", studentId)` 可能不正确
- **说明：** ServiceBooking表可能没有 `student_id` 字段，应该通过 `userId` 关联查询

---

## 📋 功能缺失问题

### 7. 地址管理功能未实现

**问题：** 地址管理功能完全缺失
- **前端：** 只有UI占位（显示"功能开发中"）
- **后端：** 没有地址管理API
- **影响：** 无法完成外卖订单的下单流程
- **状态：** ❌ 待实现

**需要实现：**
- 地址实体类 `UserAddress`
- 地址管理API（增删改查）
- 前端地址管理页面
- 下单时地址选择功能

---

### 8. 购物车后端同步缺失

**问题：** 购物车数据只存在前端本地存储
- **前端：** 有完整的购物车功能
- **后端：** 没有购物车API
- **影响：** 多设备无法同步购物车，数据容易丢失
- **状态：** ⚠️ 建议实现

---

### 9. 登录响应数据结构问题

**问题：** 前端登录代码中数据解析可能有问题
- **文件：** `seat_client/pages/login/login.js`
- **问题：** 第36行 `if (res && res.data)` 但后端返回的是 `ApiResponse<Map<String, String>>`
- **说明：** 后端返回结构是 `{ code: 200, data: { token, userId, name } }`
- **状态：** ⚠️ 需要验证

**当前代码：**
```javascript
if (res && res.data) {
    wx.setStorageSync('token', res.data.token);
    wx.setStorageSync('userId', res.data.userId);
    wx.setStorageSync('userName', res.data.name);
}
```

**应该是：**
```javascript
if (res && res.data) {
    wx.setStorageSync('token', res.data.data.token);
    wx.setStorageSync('userId', res.data.data.userId);
    wx.setStorageSync('userName', res.data.data.name);
}
```

---

## 🔧 配置问题

### 10. Java版本警告

**问题：** 项目配置使用Java 1.8，但运行环境是Java 17
- **影响：** 编译警告，可能导致兼容性问题
- **建议：** 统一Java版本配置

---

## 📊 问题优先级总结

| 优先级 | 问题 | 影响 | 状态 |
|--------|------|------|------|
| 🔴 P0 | ServiceSlot包声明错误 | 编译失败 | ✅ 已修复 |
| 🔴 P0 | ReservationServiceImpl核心逻辑未实现 | 预约功能不可用 | ❌ 待修复 |
| 🔴 P0 | 数据库字段名不匹配 | 查询失败 | ❌ 待修复 |
| 🟡 P1 | 地址管理功能缺失 | 无法完成外卖下单 | ❌ 待实现 |
| 🟡 P1 | 登录响应数据解析错误 | 登录可能失败 | ⚠️ 需验证 |
| 🟡 P2 | 购物车后端同步缺失 | 多设备无法同步 | ⚠️ 建议实现 |
| 🟢 P3 | 类型安全问题 | 编译警告 | ⚠️ 建议修复 |
| 🟢 P3 | 未使用的导入/字段 | 代码冗余 | ⚠️ 建议清理 |

---

## 🚀 修复建议

### 立即修复（P0）

1. **修复 ReservationServiceImpl**
   ```java
   // 修复字段名
   query.eq("username", studentId); // 而不是 "student_id"
   
   // 实现 reserve 方法的核心逻辑
   // 包括：锁、验证、保存、消息队列
   ```

2. **验证登录响应数据解析**
   - 测试登录接口返回的数据结构
   - 修复前端数据解析逻辑

### 短期实现（P1）

3. **实现地址管理功能**
   - 创建地址实体和表
   - 实现地址管理API
   - 创建前端地址管理页面

### 长期优化（P2-P3）

4. **实现购物车后端同步**
5. **修复类型安全问题**
6. **清理未使用的代码**

---

## 📝 测试建议

修复后需要测试：
1. ✅ 用户登录注册功能
2. ❌ 座位预约功能（需要先修复ReservationServiceImpl）
3. ❌ 订单创建功能
4. ❌ 地址管理功能（需要先实现）
5. ⚠️ 购物车功能（前端可用，但无后端同步）

---

**最后更新：** 2025-01-XX

