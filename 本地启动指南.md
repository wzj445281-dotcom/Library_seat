# 本地启动指南

## 📋 前置要求

在本地启动项目前，请确保已安装以下服务：

### 必需服务
1. **MySQL 8.0** - 数据库
   - 默认端口：3306
   - 需要创建数据库和导入表结构

2. **Redis** - 缓存服务
   - 默认端口：6379
   - 可选，但建议安装（某些功能依赖Redis）

3. **RabbitMQ** - 消息队列
   - 默认端口：5672
   - 管理界面：15672
   - 可选，但建议安装（订单处理等功能依赖）

### 开发工具
- **Java 1.8+** 和 **Maven** - 后端开发
- **微信开发者工具** - 前端开发

---

## 🚀 快速启动步骤

### 第一步：准备数据库

1. **创建数据库**
   ```sql
   CREATE DATABASE zhizuo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. **导入数据库脚本**
   
   在MySQL中执行以下SQL文件（按顺序）：
   - `seat_server/create_user_table.sql` - 创建用户表
   - `seat_server/init.sql` - 创建商品、订单等核心表
   - `seat_server/coupon_schema.sql` - 创建优惠券表（可选）
   - `seat_server/create_user_favorite_table.sql` - 创建用户收藏表（可选）

   或者使用 `init_v2.sql`（如果适用）

3. **验证数据库**
   ```sql
   USE zhizuo;
   SHOW TABLES;
   ```
   应该能看到 `user`, `products`, `orders` 等表

### 第二步：配置后端

1. **检查数据库连接配置**
   
   编辑 `seat_server/src/main/resources/application.yml`：
   ```yaml
   datasource:
     url: jdbc:mysql://localhost:3306/zhizuo?...
     username: root          # 改为你的MySQL用户名
     password: 000000        # 改为你的MySQL密码
   ```

2. **检查Redis配置**（如果已安装）
   ```yaml
   redis:
     host: localhost
     port: 6379
   ```
   如果未安装Redis，可以暂时注释掉相关依赖（但某些功能可能不可用）

3. **检查RabbitMQ配置**（如果已安装）
   ```yaml
   rabbitmq:
     host: localhost
     port: 5672
     username: guest
     password: guest
   ```
   如果未安装RabbitMQ，可以暂时注释掉相关依赖（但某些功能可能不可用）

4. **配置AI API Key（可选）**
   
   编辑 `seat_server/src/main/resources/application.yml`：
   ```yaml
   ai:
     deepseek:
       key: sk-your-actual-key-here  # 填入你的DeepSeek API Key
   ```
   如果不配置，AI功能将使用Mock模式

### 第三步：启动后端服务

1. **进入后端目录**
   ```bash
   cd seat_server
   ```

2. **启动Spring Boot应用**
   ```bash
   # 方式1：使用Maven
   mvn spring-boot:run
   
   # 方式2：使用IDE（如IntelliJ IDEA）
   # 直接运行 ZhizuoApplication.java 的 main 方法
   ```

3. **验证后端启动成功**
   - 查看控制台，应该看到 "Started ZhizuoApplication"
   - 访问 http://localhost:8080/api/app/ai/chat 测试接口
   - 或者访问 http://localhost:8080/admin.html 查看管理界面

### 第四步：配置前端

1. **配置后端地址**
   
   编辑 `seat_client/config/config.js`：
   ```javascript
   // 本地开发使用8080端口
   const BASE_URL = 'http://localhost:8080/api';
   ```

   编辑 `seat_client/utils/request.js`：
   ```javascript
   // 本地开发使用8080端口
   const BASE_URL = 'http://localhost:8080/api';
   ```

2. **真机调试配置**（重要！）
   
   ⚠️ **微信小程序无法直接访问localhost**
   
   如果要在真机上测试，必须使用局域网IP：
   ```javascript
   // 获取本机IP地址
   // Windows: 打开cmd，输入 ipconfig，找到 IPv4 地址
   // Mac/Linux: 打开终端，输入 ifconfig，找到 inet 地址
   
   // 例如：http://192.168.1.5:8080/api
   const BASE_URL = 'http://192.168.1.5:8080/api';
   ```

3. **在微信开发者工具中打开项目**
   - 打开微信开发者工具
   - 选择"导入项目"
   - 项目目录选择：`seat_client`
   - 填写AppID（可以使用测试号）
   - 点击"编译"

### 第五步：测试连接

1. **测试后端接口**
   ```bash
   # 使用curl或Postman测试
   curl http://localhost:8080/api/app/ai/chat
   ```

2. **测试前端连接**
   - 在微信开发者工具中打开小程序
   - 打开调试器，查看Console
   - 尝试登录或访问功能，查看是否有网络错误

---

## 🔧 常见问题排查

### 1. 后端启动失败 - 数据库连接错误

**错误信息：** `Communications link failure` 或 `Access denied`

**解决方案：**
- 检查MySQL服务是否启动
- 确认数据库名称是 `zhizuo`（不是 `zhizuo_db`）
- 检查用户名密码是否正确
- 确认端口是3306

### 2. 后端启动失败 - Redis连接错误

**错误信息：** `Unable to connect to Redis`

**解决方案：**
- 如果不需要Redis功能，可以暂时注释掉相关配置
- 或者安装并启动Redis服务
- 检查Redis是否在6379端口运行

### 3. 后端启动失败 - RabbitMQ连接错误

**错误信息：** `Connection refused` 或 `Authentication failed`

**解决方案：**
- 如果不需要RabbitMQ功能，可以暂时注释掉相关配置
- 或者安装并启动RabbitMQ服务
- 检查RabbitMQ是否在5672端口运行
- 确认用户名密码是 guest/guest

### 4. 前端无法连接后端

**错误信息：** `网络连接失败` 或 `request:fail`

**解决方案：**
- 确认后端已成功启动（查看后端控制台）
- 检查 `config.js` 和 `request.js` 中的 `BASE_URL` 是否正确
- 真机调试时，确保使用局域网IP而非localhost
- 检查防火墙是否阻止了8080端口
- 确保手机和电脑在同一局域网

### 5. 数据库表不存在

**错误信息：** `Table 'zhizuo.xxx' doesn't exist`

**解决方案：**
- 检查是否已执行数据库初始化脚本
- 确认数据库名称是 `zhizuo`
- 重新执行 `init.sql` 等脚本

---

## 📝 本地开发配置检查清单

启动前，请确认：

- [ ] MySQL已安装并启动，端口3306
- [ ] 已创建数据库 `zhizuo`
- [ ] 已导入数据库表结构（执行了init.sql等脚本）
- [ ] `application.yml` 中数据库用户名密码正确
- [ ] Redis已安装并启动（可选，端口6379）
- [ ] RabbitMQ已安装并启动（可选，端口5672）
- [ ] 后端可以成功启动（查看控制台日志）
- [ ] 前端 `config.js` 和 `request.js` 中的 `BASE_URL` 配置正确
- [ ] 真机调试时使用了局域网IP而非localhost

---

## 🎯 快速测试

启动成功后，可以测试以下功能：

1. **后端健康检查**
   - 访问：http://localhost:8080/admin.html
   - 应该能看到管理界面

2. **API接口测试**
   ```bash
   curl -X POST http://localhost:8080/api/app/ai/chat \
     -H "Content-Type: application/json" \
     -d '{"message":"你好"}'
   ```

3. **前端登录测试**
   - 在微信开发者工具中打开小程序
   - 尝试登录功能
   - 查看控制台是否有错误

---

## 💡 提示

1. **开发环境建议使用本地启动**，而不是Docker，这样调试更方便

2. **真机调试时**，确保：
   - 手机和电脑在同一WiFi网络
   - 使用局域网IP而非localhost
   - 防火墙允许8080端口访问

3. **如果某些服务未安装**（如Redis、RabbitMQ），可以暂时注释掉相关配置，但某些功能可能不可用

4. **AI功能**如果不配置API Key，会使用Mock模式，可以正常演示但不会调用真实AI

---

**最后更新：** 2025-01-XX

