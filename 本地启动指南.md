# 瑞幸咖啡小程序 - 本地启动指南（不使用 Docker）

## 📋 环境准备

### 必需软件
- [ ] **Java 1.8+** 和 **Maven 3.6+**
- [ ] **MySQL 8.0**（本地安装，端口 3306）
- [ ] **Redis**（本地安装，端口 6379）
- [ ] **RabbitMQ**（可选，如果不需要消息队列功能可以不安装）
- [ ] **微信开发者工具**

---

## 🗄️ 第一步：数据库配置（5分钟）

### 1.1 创建数据库

打开 MySQL 客户端（Navicat、DBeaver 或命令行），执行：

```sql
CREATE DATABASE ruixing CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

或者使用 `zhizuo` 数据库（如果已存在）：

```sql
USE zhizuo;
```

### 1.2 执行 SQL 初始化脚本

按顺序执行以下 SQL 文件（在 `seat_server/` 目录下）：

1. `init.sql` - 创建商品、订单等核心表
2. `create_user_table.sql` - 创建用户表
3. `create_user_address_table.sql` - 创建地址表
4. `create_user_favorite_table.sql` - 创建收藏表
5. `coupon_schema.sql` - 创建优惠券表
6. `alter_orders_table.sql` - 修改订单表结构

**执行方式：**
- 使用 Navicat/DBeaver：直接打开 SQL 文件执行
- 使用命令行：
```bash
mysql -uroot -p ruixing < seat_server/init.sql
mysql -uroot -p ruixing < seat_server/create_user_table.sql
# ... 依次执行其他文件
```

---

## ☕ 第二步：配置后端（2分钟）

### 2.1 修改数据库配置

编辑 `seat_server/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ruixing?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 你的MySQL密码  # 修改这里
```

### 2.2 配置 Redis（如果已安装）

```yaml
spring:
  redis:
    host: localhost
    port: 6379
    password: 你的Redis密码  # 如果没有密码，删除这行
```

### 2.3 配置 DeepSeek API Key（可选）

```yaml
ai:
  deepseek:
    key: sk-your-actual-key-here  # 填入你的 DeepSeek API Key
```

如果不配置，AI 功能将使用 Mock 模式。

### 2.4 禁用 RabbitMQ（如果未安装）

如果本地没有安装 RabbitMQ，保持以下配置（已禁用）：

```yaml
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
```

---

## 🚀 第三步：启动后端服务（3分钟）

### 3.1 进入后端目录

```bash
cd seat_server
```

### 3.2 安装依赖并启动

**方式一：使用 Maven 直接运行**

```bash
mvn spring-boot:run
```

**方式二：先打包再运行**

```bash
# 打包
mvn clean package -DskipTests

# 运行
java -jar target/zhizuo-0.0.1-SNAPSHOT.jar
```

### 3.3 验证后端启动成功

看到以下日志说明启动成功：

```
Started ZhizuoApplication in X.XXX seconds
```

**访问测试：**
- API 文档：http://localhost:8080/doc.html
- 后台管理：http://localhost:8080/admin.html

---

## 📱 第四步：配置前端（2分钟）

### 4.1 获取本机 IP 地址

**Windows：**
```bash
ipconfig
# 找到 "IPv4 地址"，例如：192.168.1.100
```

**Mac/Linux：**
```bash
ifconfig
# 找到 "inet"，例如：192.168.1.100
```

### 4.2 修改前端 API 地址

编辑 `seat_client/utils/request.js`，第 7 行：

```javascript
// 修改前
const BASE_URL = 'http://localhost:8080/api';

// 修改后（使用你的局域网 IP）
const BASE_URL = 'http://192.168.1.100:8080/api';
```

**重要：** 
- 微信小程序**不能使用** `localhost` 或 `127.0.0.1`
- 必须使用**局域网 IP 地址**
- 端口是 `8080`（本地启动）或 `8090`（如果修改了端口）

---

## 🎯 第五步：启动微信小程序（1分钟）

### 5.1 打开微信开发者工具

1. 打开微信开发者工具
2. 选择"导入项目"
3. 选择项目目录：`seat_client` 文件夹
4. 填写 AppID（可以使用测试号）

### 5.2 编译运行

1. 点击"编译"按钮
2. 等待编译完成
3. 在模拟器中测试功能

---

## ✅ 验证功能

### 后端验证

1. **访问 API 文档**
   - 打开浏览器：http://localhost:8080/doc.html
   - 应该能看到所有 API 接口

2. **测试登录接口**
   - 在 API 文档中找到 `POST /api/auth/send-code`
   - 测试发送验证码

### 前端验证

1. **登录功能**
   - 输入手机号
   - 点击"获取验证码"
   - 查看后端日志，找到验证码
   - 输入验证码登录

2. **菜单浏览**
   - 登录后应该能看到商品列表
   - 可以切换分类
   - 可以收藏商品

---

## ⚠️ 常见问题

### 问题1：后端启动失败 - 数据库连接错误

**错误信息：** `Communications link failure` 或 `Access denied`

**解决方案：**
1. 检查 MySQL 是否启动：`net start MySQL`（Windows）或 `systemctl status mysql`（Linux）
2. 检查数据库名称是否正确：`ruixing` 或 `zhizuo`
3. 检查用户名密码是否正确
4. 检查端口是否为 3306

### 问题2：后端启动失败 - Redis 连接错误

**错误信息：** `Unable to connect to Redis`

**解决方案：**
1. 检查 Redis 是否启动：
   - Windows：查看服务列表
   - Mac/Linux：`redis-cli ping`（应该返回 `PONG`）
2. 如果没有安装 Redis，可以暂时注释掉 Redis 相关配置
3. 或者安装 Redis：
   - Windows：下载 Redis for Windows
   - Mac：`brew install redis`
   - Linux：`sudo apt-get install redis-server`

### 问题3：小程序无法连接后端

**错误信息：** "网络连接失败"

**解决方案：**
1. 检查 `BASE_URL` 是否使用局域网 IP（不能是 localhost）
2. 检查后端是否正常运行：访问 http://你的IP:8080/doc.html
3. 检查防火墙是否阻止了 8080 端口
4. 确保手机/模拟器和电脑在同一个局域网

### 问题4：验证码发送失败

**错误信息：** "网络服务异常"

**解决方案：**
1. 检查 Redis 是否正常运行
2. 检查后端日志，查看具体错误
3. 验证码存储在 Redis 中，Key 格式：`sms:code:手机号`

### 问题5：端口被占用

**错误信息：** `Port 8080 is already in use`

**解决方案：**
1. 查找占用端口的进程：
   ```bash
   # Windows
   netstat -ano | findstr :8080
   
   # Mac/Linux
   lsof -i :8080
   ```
2. 结束进程或修改 `application.yml` 中的端口：
   ```yaml
   server:
     port: 8081  # 改为其他端口
   ```
3. 记得同步修改前端的 `BASE_URL` 端口号

---

## 📝 快速启动命令总结

### 后端启动

```bash
cd seat_server
mvn spring-boot:run
```

### 前端启动

1. 打开微信开发者工具
2. 导入 `seat_client` 目录
3. 点击编译

---

## 🎉 完成！

如果后端和前端都能正常启动，就可以开始演示了！

**演示流程：**
1. 登录注册 → 发送验证码 → 注册/登录
2. 浏览商品 → 收藏 → 加入购物车
3. 地址管理 → 新增地址
4. 下单 → 选择地址 → 选择优惠券 → 提交订单
5. 订单管理 → 支付 → 查看取餐码
6. AI助手 → 对话 → 商品推荐

如有问题，请查看后端日志或前端控制台错误信息。
