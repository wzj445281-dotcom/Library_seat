# 瑞幸咖啡数据初始化说明

## 📋 文件说明

已根据你提供的数据创建了适配的 SQL 初始化文件：`seat_server/init_ruixing_data.sql`

## ✅ 已调整的内容

### 1. 表名调整
- ✅ `category` → `categories` (复数)
- ✅ `product` → `products` (复数)
- ✅ `coupon` 和 `user_coupon` 保持不变（单数）

### 2. 字段调整

#### 用户表 (`user`)
- ✅ 移除了 `openid`, `nickname`, `avatar` 字段（项目中使用 `username`, `name`, `phone`）
- ✅ 密码使用 BCrypt 加密存储（默认密码：`123456`）
- ✅ 添加了 `points`（积分）和 `role`（角色）字段

#### 分类表 (`categories`)
- ✅ 移除了 `icon` 字段（项目中使用 `name` 和 `sort`）

#### 商品表 (`products`)
- ✅ `image` → `img_url`
- ✅ 添加了 `sales`（销量）字段

#### 优惠券表 (`coupon`)
- ✅ `name` → `title`
- ✅ `type`, `value`, `min_amount` → `amount`, `min_point`
- ✅ 移除了 `start_time`, `end_time`（项目中使用 `stock`, `total_stock`, `status`）

#### 座位表 (`seat`)
- ✅ 提供了两种方案：
  - **方案1**：使用 `grid_x`, `grid_y`, `label` 字段（根据 Seat 实体类，推荐）
  - **方案2**：使用 `store_id`, `seat_number`, `type` 字段（如果表结构不同）

## 🚀 使用方法

### 方法1：使用 Navicat/DBeaver（推荐）

1. 打开数据库工具，连接到 `zhizuo` 数据库
2. 打开 `seat_server/init_ruixing_data.sql` 文件
3. 执行整个 SQL 脚本

### 方法2：使用 MySQL 命令行

```bash
mysql -uroot -p000000 zhizuo < seat_server/init_ruixing_data.sql
```

### 方法3：手动执行

1. 打开 `seat_server/init_ruixing_data.sql` 文件
2. 复制 SQL 内容
3. 在数据库工具中执行

## ⚠️ 注意事项

### 1. 数据清理
- SQL 文件中包含了 `TRUNCATE TABLE` 语句，会清空现有数据
- 如果不想清空数据，请注释掉相应的 `TRUNCATE` 语句
- 用户数据使用了 `ON DUPLICATE KEY UPDATE`，如果用户已存在会更新，不会报错

### 2. 密码说明
- 默认密码：`123456`
- 密码已使用 BCrypt 加密存储
- 加密值：`$2a$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi`
- 测试账号：
  - 管理员：`admin` / `123456`
  - 普通用户：`user` / `123456`

### 3. 座位表字段
- 如果座位表结构不同，请根据实际情况选择方案1或方案2
- 方案1使用 `grid_x`, `grid_y`, `label`（推荐，匹配 Seat 实体类）
- 方案2使用 `store_id`, `seat_number`, `type`（如果表结构不同）

### 4. 优惠券数据
- `user_coupon` 表中的 `user_id` 字段类型为 `String`，所以使用 `'1'`, `'2'` 而不是 `1`, `2`

## 📊 初始化后的数据

### 用户数据
- ✅ 2 个测试用户（admin 和 user）

### 分类数据
- ✅ 4 个分类（大师咖啡、生椰家族、瑞纳冰、经典甜点）

### 商品数据
- ✅ 7 个咖啡商品（标准美式、拿铁、卡布奇诺、生椰拿铁、椰云拿铁、抹茶瑞纳冰、巧克力瑞纳冰）

### 座位数据
- ✅ 7 个座位（A01-A03, B01-B02, C01-C02）

### 优惠券数据
- ✅ 3 张优惠券（新人全场5折券、满30减15、咖啡买一赠一）
- ✅ 3 张用户优惠券（分配给测试用户）

## 🔍 验证数据

执行 SQL 后，可以使用以下 SQL 验证数据：

```sql
-- 查看用户数据
SELECT id, username, name, phone, role FROM `user`;

-- 查看分类数据
SELECT * FROM `categories`;

-- 查看商品数据
SELECT id, category_id, name, price, stock FROM `products`;

-- 查看座位数据
SELECT * FROM `seat`;

-- 查看优惠券数据
SELECT * FROM `coupon`;

-- 查看用户优惠券
SELECT * FROM `user_coupon`;
```

## 📝 后续操作

1. **测试登录**：使用 `admin` / `123456` 或 `user` / `123456` 登录
2. **查看商品**：在小程序中查看咖啡商品列表
3. **测试下单**：创建测试订单
4. **测试优惠券**：使用分配的优惠券

---

**文件位置**：`seat_server/init_ruixing_data.sql`

