# 瑞幸风 O2O 电商小程序 - 开发任务清单

## 📋 任务概览

基于项目现状，以下是下一步开发任务的详细清单和实现方案。

---

## 🎯 任务一：完善优惠券核销功能 ⭐⭐⭐

### 当前状态
- ✅ 后端已有优惠券领取逻辑（CouponController）
- ✅ 后端已有订单结算服务（OrderSettlementService）
- ✅ 前端有优惠券页面（coupon/index.js）
- ❌ **缺失**：结算页面没有优惠券选择功能
- ❌ **缺失**：订单创建时没有集成优惠券结算

### 需要实现的功能

#### 1.1 后端增强

**1.1.1 完善订单创建接口**
- 文件：`seat_server/src/main/java/com/example/zhizuo/core/service/impl/OrderServiceImpl.java`
- 功能：
  - 从数据库查询商品价格（防止前端篡改）
  - 计算订单原价
  - 调用 `OrderSettlementService` 计算优惠后价格
  - 保存订单时记录优惠券ID和优惠金额
  - 使用优惠券后更新 `UserCoupon` 状态

**1.1.2 添加获取可用优惠券接口**
- 文件：`seat_server/src/main/java/com/example/zhizuo/api/app/CouponController.java`
- 接口：`GET /api/app/coupon/available?totalPrice=100`
- 功能：根据订单金额筛选可用优惠券（满足门槛且未使用）

**1.1.3 完善订单实体**
- 文件：`seat_server/src/main/java/com/example/zhizuo/core/entity/Order.java`
- 添加字段：
  - `couponId` - 使用的优惠券ID
  - `discountAmount` - 优惠金额
  - `originalAmount` - 原价

#### 1.2 前端增强

**1.2.1 结算页面添加优惠券选择**
- 文件：`seat_client/pages/orders/checkout/checkout.js`
- 功能：
  - 加载可用优惠券列表
  - 显示优惠券选择器
  - 实时计算优惠后价格
  - 提交订单时传递优惠券ID

**1.2.2 结算页面UI优化**
- 文件：`seat_client/pages/orders/checkout/checkout.wxml`
- 添加：
  - 优惠券选择卡片
  - 价格明细展示（原价、优惠、实付）

---

## 🎯 任务二：实现地址管理功能 ⭐⭐⭐

### 当前状态
- ❌ 完全缺失，只有UI占位

### 需要实现的功能

#### 2.1 后端实现

**2.1.1 创建地址实体**
- 文件：`seat_server/src/main/java/com/example/zhizuo/core/entity/UserAddress.java`
- 字段：
  - `id`, `userId`
  - `name` (收货人姓名)
  - `phone` (收货人电话)
  - `province`, `city`, `district` (省市区)
  - `detail` (详细地址)
  - `isDefault` (是否默认地址)
  - `latitude`, `longitude` (经纬度，用于地图定位)

**2.1.2 创建地址Mapper**
- 文件：`seat_server/src/main/java/com/example/zhizuo/core/mapper/UserAddressMapper.java`

**2.1.3 创建地址Service**
- 文件：`seat_server/src/main/java/com/example/zhizuo/core/service/UserAddressService.java`
- 方法：
  - `list()` - 获取地址列表
  - `add()` - 添加地址
  - `update()` - 更新地址
  - `delete()` - 删除地址
  - `setDefault()` - 设置默认地址

**2.1.4 创建地址Controller**
- 文件：`seat_server/src/main/java/com/example/zhizuo/api/app/UserAddressController.java`
- 接口：
  - `GET /api/app/address/list` - 获取地址列表
  - `POST /api/app/address/add` - 添加地址
  - `PUT /api/app/address/update/{id}` - 更新地址
  - `DELETE /api/app/address/delete/{id}` - 删除地址
  - `PUT /api/app/address/set-default/{id}` - 设置默认地址

**2.1.5 创建数据库表**
- 文件：`seat_server/create_user_address_table.sql`

#### 2.2 前端实现

**2.2.1 地址列表页**
- 文件：`seat_client/pages/address/list.js`, `list.wxml`, `list.wxss`
- 功能：
  - 显示地址列表
  - 标记默认地址
  - 编辑/删除操作
  - 选择地址（用于下单）

**2.2.2 地址编辑页**
- 文件：`seat_client/pages/address/edit.js`, `edit.wxml`, `edit.wxss`
- 功能：
  - 表单输入（姓名、电话、地址）
  - 集成微信地图选择位置
  - 省市区选择器
  - 设置默认地址开关

**2.2.3 集成到结算页面**
- 文件：`seat_client/pages/orders/checkout/checkout.js`
- 功能：
  - 外卖模式下显示地址选择
  - 跳转到地址列表选择
  - 显示选中的地址信息

**2.2.4 集成到"我的"页面**
- 文件：`seat_client/pages/mine/mine.js`
- 功能：
  - 地址管理菜单项可点击
  - 跳转到地址列表页

---

## 🎯 任务三：性能优化 ⭐⭐

### 3.1 图片懒加载

**3.1.1 实现懒加载组件**
- 文件：`seat_client/components/lazy-image/lazy-image.js`
- 功能：
  - 使用 `IntersectionObserver` API
  - 图片进入视口时加载
  - 显示占位图

**3.1.2 应用到商品列表**
- 文件：`seat_client/pages/menu/index.wxml`
- 替换所有 `<image>` 为 `<lazy-image>`

### 3.2 分包策略

**3.2.1 配置分包**
- 文件：`seat_client/app.json`
- 分包结构：
  ```
  pages/
    ├── index/          # 主包
    ├── menu/
    ├── mine/
    └── orders/
  
  subpackages/
    ├── address/        # 地址管理分包
    ├── coupon/         # 优惠券分包
    └── ai_chat/        # AI对话分包
  ```

**3.2.2 优化首屏加载**
- 减少主包体积
- 按需加载分包

---

## 📊 开发优先级

| 优先级 | 任务 | 预计工时 | 影响 |
|--------|------|----------|------|
| P0 | 优惠券核销功能 | 4-6小时 | 核心业务闭环 |
| P0 | 地址管理功能 | 6-8小时 | 外卖下单必需 |
| P1 | 图片懒加载 | 2-3小时 | 用户体验优化 |
| P2 | 分包策略 | 1-2小时 | 性能优化 |

---

## 🚀 开始实现

我将按照优先级逐步实现这些功能。首先从**优惠券核销功能**开始**。

