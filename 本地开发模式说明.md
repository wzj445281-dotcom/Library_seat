# 本地开发模式说明

## ✅ 已切换为本地开发模式

已将前端配置改回 `http://localhost:8080/api`，现在可以在**微信开发者工具的模拟器**中正常使用。

## 🎯 使用方式

### 1. 启动后端服务

```bash
cd E:\Coding\Wechat_project\bookSeats\seat_server
mvn spring-boot:run
```

等待看到：
```
Started ZhizuoApplication in X.XXX seconds
```

### 2. 在微信开发者工具中测试

1. **打开微信开发者工具**
2. **导入项目**：选择 `seat_client` 目录
3. **点击"编译"**，在模拟器中测试
4. **验证码功能**：
   - 输入手机号（例如：13800138000）
   - 点击"获取验证码"
   - 查看后端控制台，会输出验证码
   - 例如：`【瑞幸咖啡】验证码：123456，5分钟内有效。请勿泄露给他人。`

## 📝 重要说明

### ✅ 模拟器支持 localhost

- **微信开发者工具的模拟器**支持 `localhost` 和 `127.0.0.1`
- 可以直接使用 `http://localhost:8080/api`
- **无需配置IP地址**

### ❌ 真机不支持 localhost

- **真机调试**时，必须使用局域网IP（如 `http://192.168.78.196:8080/api`）
- 真机无法访问 `localhost`（因为真机的 localhost 指向真机本身，不是电脑）

### 🔄 切换模式

如果需要切换回真机调试：

1. **修改 `seat_client/utils/request.js` 第7行**：
   ```javascript
   const BASE_URL = 'http://192.168.78.196:8080/api';  // 使用你的实际IP
   ```

2. **获取电脑IP**：
   ```bash
   ipconfig
   ```

3. **确认真机和电脑在同一WiFi**

## 🚀 测试验证码功能

### 步骤1：启动后端

```bash
cd seat_server
mvn spring-boot:run
```

### 步骤2：在模拟器中测试

1. 打开登录页面
2. 输入手机号：`13800138000`
3. 点击"获取验证码"
4. **查看后端控制台**，应该看到：
   ```
   【瑞幸咖啡】验证码：123456，5分钟内有效。请勿泄露给他人。
   ```
5. 在登录页面输入这个验证码
6. 点击"登录"

### 步骤3：验证登录

- 应该看到"登录成功"提示
- 自动跳转到首页
- 可以正常使用其他功能

## 🔍 常见问题

### Q1: 点击"获取验证码"没有反应？

**检查：**
1. 后端是否启动（查看控制台）
2. Redis是否运行（验证码存储在Redis中）
3. 查看微信开发者工具的控制台，是否有错误信息

### Q2: 显示"网络连接失败"？

**检查：**
1. 后端是否在 8080 端口运行
2. 在浏览器访问：`http://localhost:8080/doc.html`
   - 能打开 → 后端正常
   - 打不开 → 后端未启动或端口被占用

### Q3: 验证码在哪里查看？

**开发模式下**，验证码会输出到**后端控制台**：
```
【瑞幸咖啡】验证码：123456，5分钟内有效。请勿泄露给他人。
```

**生产环境**需要接入真实的短信服务。

## ✅ 当前配置

- **BASE_URL**: `http://localhost:8080/api`
- **后端端口**: `8080`
- **Redis**: `localhost:6379`
- **数据库**: `localhost:3306/zhizuo`

## 🎉 现在可以正常使用了！

在微信开发者工具的模拟器中，所有功能都应该可以正常使用，包括：
- ✅ 获取验证码
- ✅ 登录/注册
- ✅ 浏览商品
- ✅ 下单
- ✅ 查看订单
- ✅ 地址管理
- ✅ 优惠券
- ✅ AI助手

享受开发吧！🚀

