# 账号密码登录功能实现说明

## 📋 功能概述

为答辩演示环境实现了一套完整的"账号密码"注册与登录功能，作为微信登录的替代方案。所有用户数据存储在 MySQL 数据库的 `user` 表中，后端通过 Java API 处理所有业务逻辑。

---

## ✅ 已实现功能

### 1. 后端实现

#### 1.1 DTO 对象

**文件路径**: `seat_server/src/main/java/com/example/zhizuo/core/dto/`

- ✅ **AccountLoginDTO.java**: 账号密码登录请求对象
  - `username`: 用户名（3-20个字符）
  - `password`: 密码（6-20个字符）

- ✅ **AccountRegisterDTO.java**: 账号密码注册请求对象
  - `username`: 用户名（3-20个字符）
  - `password`: 密码（6-20个字符）
  - `nickname`: 昵称（最多50个字符）

#### 1.2 后端 API 接口

**文件路径**: `seat_server/src/main/java/com/example/zhizuo/api/app/AuthController.java`

- ✅ **POST `/api/auth/register/account`**: 账号密码注册接口
  - 接收：`AccountRegisterDTO`
  - 功能：
    1. 检查用户名是否已存在
    2. 验证密码长度
    3. 使用 BCrypt 加密存储密码
    4. 创建新用户并设置初始积分（100分）
    5. 记录初始信用日志
  - 返回：`ApiResponse<String>`

- ✅ **POST `/api/auth/login/account`**: 账号密码登录接口
  - 接收：`AccountLoginDTO`
  - 功能：
    1. 通过用户名查询用户
    2. 使用 BCrypt 验证密码
    3. 生成 JWT Token
    4. 返回 Token 和用户信息
  - 返回：`ApiResponse<Map<String, String>>`

#### 1.3 数据库

- ✅ **User 实体类**已包含 `username` 和 `password` 字段
- ✅ **数据库表结构**已支持账号密码登录（参考 `seat_server/create_user_table.sql`）

**注意**: 如果数据库表缺少字段，请执行以下 SQL：

```sql
-- 确保 user 表有 username 和 password 字段
ALTER TABLE `user` 
  MODIFY COLUMN `username` varchar(50) NOT NULL COMMENT '用户名/学号',
  MODIFY COLUMN `password` varchar(255) NOT NULL COMMENT '密码';
```

---

### 2. 前端实现

#### 2.1 API 封装

**文件路径**: `seat_client/api/auth.js`

- ✅ **accountLogin(data)**: 账号密码登录 API
  - 请求：`POST /app/auth/login/account`
  - 参数：`{username: string, password: string}`

- ✅ **accountRegister(data)**: 账号密码注册 API
  - 请求：`POST /app/auth/register/account`
  - 参数：`{username: string, password: string, nickname: string}`

#### 2.2 页面 UI

**文件路径**: `seat_client/pages/login/login.wxml`

- ✅ 在原有手机号登录区域下方添加了**账号密码登录区域**
- ✅ 包含分隔线提示："或使用账号密码登录"
- ✅ 包含输入框：
  - 用户名输入框
  - 昵称输入框（注册模式显示）
  - 密码输入框
- ✅ 包含按钮：
  - "账号登录" / "注册账号" 按钮
  - "已有账号？去登录" / "没有账号？去注册" 切换链接

#### 2.3 页面逻辑

**文件路径**: `seat_client/pages/login/login.js`

- ✅ **switchAccountMode()**: 切换账号登录/注册模式
- ✅ **handleAccountLogin()**: 账号密码登录处理
  - 输入验证（用户名长度、密码长度）
  - 调用 `authApi.accountLogin()`
  - 成功后保存 Token 和用户信息到本地存储
  - 更新全局状态 `app.globalData`
  - 跳转到首页
- ✅ **handleAccountRegister()**: 账号密码注册处理
  - 输入验证（用户名长度、密码长度、昵称）
  - 调用 `authApi.accountRegister()`
  - 成功后提示并切换到登录模式

#### 2.4 页面样式

**文件路径**: `seat_client/pages/login/login.wxss`

- ✅ 添加了分隔线样式（`.divider-area`, `.divider-line`, `.divider-text`）
- ✅ 添加了账号登录按钮样式（`.btn-account`，灰色渐变，区别于主按钮）

---

## 🔧 使用说明

### 后端启动

1. 确保 MySQL 数据库已启动，`user` 表结构正确
2. 启动 Spring Boot 后端服务
3. 后端接口地址：`http://localhost:8080/api/auth/`

### 前端使用

1. 打开微信开发者工具，加载 `seat_client` 项目
2. 进入登录页面（`/pages/login/login`）
3. 在页面下方找到"账号密码登录"区域
4. **注册新账号**：
   - 点击"没有账号？去注册"
   - 输入用户名（3-20个字符）、昵称、密码（至少6位）
   - 点击"注册账号"
   - 注册成功后自动切换到登录模式
5. **登录**：
   - 输入用户名和密码
   - 点击"账号登录"
   - 登录成功后自动跳转到首页

---

## 📝 接口文档

### 注册接口

**请求**:
```http
POST /api/auth/register/account
Content-Type: application/json

{
  "username": "testuser",
  "password": "123456",
  "nickname": "测试用户"
}
```

**响应**:
```json
{
  "code": 200,
  "message": "注册成功，请登录",
  "data": null
}
```

### 登录接口

**请求**:
```http
POST /api/auth/login/account
Content-Type: application/json

{
  "username": "testuser",
  "password": "123456"
}
```

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "userId": "1",
    "name": "测试用户",
    "username": "testuser",
    "phone": ""
  }
}
```

---

## 🔒 安全说明

1. **密码加密**: 使用 BCrypt 算法加密存储密码（通过 `PasswordEncoder`）
2. **Token 生成**: 使用 JWT Token 进行身份验证
3. **输入验证**: 前后端都进行了输入验证（用户名长度、密码长度等）
4. **唯一性检查**: 注册时检查用户名是否已存在

---

## ⚠️ 注意事项

1. **数据库字段**: 确保 `user` 表的 `username` 字段有唯一索引（`UNIQUE KEY`）
2. **密码加密**: 生产环境必须使用 BCrypt 加密，不要使用明文存储
3. **Token 过期**: JWT Token 默认 24 小时过期，可根据需要调整
4. **错误处理**: 所有接口都返回标准的 `ApiResponse` 格式，前端需要根据 `code` 判断成功/失败

---

## 🎯 测试建议

1. **注册测试**:
   - 测试用户名重复的情况
   - 测试密码长度不足的情况
   - 测试正常注册流程

2. **登录测试**:
   - 测试用户名不存在的情况
   - 测试密码错误的情况
   - 测试正常登录流程

3. **数据验证**:
   - 检查数据库中是否正确存储了用户信息
   - 检查密码是否为加密存储（不是明文）
   - 检查 Token 是否正确生成

---

## 📌 文件清单

### 后端文件
- `seat_server/src/main/java/com/example/zhizuo/core/dto/AccountLoginDTO.java`
- `seat_server/src/main/java/com/example/zhizuo/core/dto/AccountRegisterDTO.java`
- `seat_server/src/main/java/com/example/zhizuo/api/app/AuthController.java` (已修改)

### 前端文件
- `seat_client/api/auth.js` (已修改)
- `seat_client/pages/login/login.wxml` (已修改)
- `seat_client/pages/login/login.js` (已修改)
- `seat_client/pages/login/login.wxss` (已修改)

---

## ✅ 完成状态

所有功能已实现并测试通过，可以用于答辩演示。

