# 数据库初始化检查结果

## ✅ 已存在的表和数据

### 表结构
数据库 `zhizuo` 中已存在以下表：
- ✅ `categories` - 商品分类表
- ✅ `products` - 商品表（有4条数据）
- ✅ `orders` - 订单表
- ✅ `order_items` - 订单明细表
- ✅ `user` - 用户表（有2条数据）
- ✅ `coupon` - 优惠券表
- ✅ `user_coupon` - 用户优惠券表
- ✅ `feedback` - 反馈表
- ✅ `seat` - 座位表
- ✅ `work_order` - 工单表

### 数据情况
- ✅ **商品数据**：4条（已初始化）
- ✅ **用户数据**：2条（已初始化）
- ✅ **分类数据**：需要检查

## ⚠️ 可能缺失的表

根据项目需求，以下表可能需要检查：

1. **user_address** - 用户地址表（地址管理功能需要）
2. **user_favorite** - 用户收藏表（收藏功能需要）

## 🔧 如果缺少表，执行以下SQL

### 1. 创建用户地址表

执行 `seat_server/create_user_address_table.sql`：

```sql
CREATE TABLE IF NOT EXISTS `user_address` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL COMMENT '用户ID',
  `name` varchar(50) NOT NULL COMMENT '收货人姓名',
  `phone` varchar(20) NOT NULL COMMENT '收货人电话',
  `province` varchar(50) DEFAULT NULL COMMENT '省份',
  `city` varchar(50) DEFAULT NULL COMMENT '城市',
  `district` varchar(50) DEFAULT NULL COMMENT '区县',
  `detail` varchar(255) NOT NULL COMMENT '详细地址',
  `is_default` tinyint(1) DEFAULT 0 COMMENT '是否默认地址 0:否 1:是',
  `latitude` decimal(10,7) DEFAULT NULL COMMENT '纬度',
  `longitude` decimal(10,7) DEFAULT NULL COMMENT '经度',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_is_default` (`user_id`, `is_default`)
) ENGINE=InnoDB COMMENT='用户收货地址表';
```

### 2. 创建用户收藏表

执行 `seat_server/create_user_favorite_table.sql`：

```sql
CREATE TABLE IF NOT EXISTS `user_favorite` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` varchar(64) NOT NULL COMMENT '用户ID',
  `product_id` bigint(20) NOT NULL COMMENT '商品ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_product` (`user_id`,`product_id`) COMMENT '防止重复收藏'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户收藏表';
```

### 3. 检查订单表结构

如果订单表缺少优惠券相关字段，执行 `seat_server/alter_orders_table.sql`：

```sql
ALTER TABLE `orders` 
ADD COLUMN IF NOT EXISTS `coupon_id` bigint(20) DEFAULT NULL COMMENT '使用的优惠券ID' AFTER `address_info`,
ADD COLUMN IF NOT EXISTS `discount_amount` decimal(10,2) DEFAULT 0.00 COMMENT '优惠金额' AFTER `coupon_id`,
ADD COLUMN IF NOT EXISTS `original_amount` decimal(10,2) DEFAULT NULL COMMENT '订单原价（优惠前）' AFTER `discount_amount`;
```

## 📝 快速检查命令

### 检查所有表
```bash
mysql -uroot -p000000 zhizuo -e "SHOW TABLES;"
```

### 检查数据量
```bash
# 检查商品数量
mysql -uroot -p000000 zhizuo -e "SELECT COUNT(*) FROM products;"

# 检查用户数量
mysql -uroot -p000000 zhizuo -e "SELECT COUNT(*) FROM user;"

# 检查分类数量
mysql -uroot -p000000 zhizuo -e "SELECT COUNT(*) FROM categories;"
```

### 检查特定表是否存在
```bash
# 检查地址表
mysql -uroot -p000000 zhizuo -e "SHOW TABLES LIKE 'user_address';"

# 检查收藏表
mysql -uroot -p000000 zhizuo -e "SHOW TABLES LIKE 'user_favorite';"
```

## 🚀 执行SQL脚本的方法

### 方法1：使用命令行
```bash
mysql -uroot -p000000 zhizuo < seat_server/create_user_address_table.sql
mysql -uroot -p000000 zhizuo < seat_server/create_user_favorite_table.sql
mysql -uroot -p000000 zhizuo < seat_server/alter_orders_table.sql
```

### 方法2：使用 Navicat/DBeaver
1. 连接到 `zhizuo` 数据库
2. 打开 SQL 文件
3. 执行 SQL

### 方法3：使用 MySQL 命令行
```bash
mysql -uroot -p000000 zhizuo
```
然后复制粘贴 SQL 语句执行。

## ✅ 验证

执行完 SQL 后，重启后端服务，应该可以正常启动了。

如果还有问题，检查后端日志中的具体错误信息。

