# 网络连接问题完整排查指南

## 🔍 问题现象

在微信开发者工具模拟器中无法连接网络，显示"网络连接失败"。

## ✅ 已完成的修复

### 1. 修改 BASE_URL
- 从 `localhost` 改为 `127.0.0.1`

### 2. 添加 CORS 配置
- 在 `WebMvcConfig.java` 中添加了 CORS 跨域配置
- 允许所有来源访问 API（开发环境）

### 3. 后端服务状态
- ✅ 后端服务正在运行（8080端口已监听）

## 🔧 完整排查步骤

### 步骤1：确认后端服务运行

**检查方法：**
```bash
netstat -ano | findstr :8080
```

**应该看到：**
```
TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING
```

**如果未运行，启动后端：**
```bash
cd seat_server
mvn spring-boot:run
```

### 步骤2：在浏览器中测试后端

**打开浏览器，访问：**
```
http://127.0.0.1:8080/doc.html
```

**结果判断：**
- ✅ **能打开** → 后端正常，问题在微信开发者工具
- ❌ **打不开** → 后端未启动或端口被占用

### 步骤3：配置微信开发者工具（最重要！）

**必须配置：**

1. **打开微信开发者工具**
2. **点击右上角"详情"按钮**
3. **切换到"本地设置"标签**
4. **勾选以下选项：**
   - ✅ **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**
   - ✅ **不校验安全域名（TLS版本）**

**这是最关键的一步！如果不勾选，所有网络请求都会被拦截！**

### 步骤4：清除缓存重新编译

1. **点击"编译"旁边的下拉箭头**
2. **选择"清除缓存"**
3. **重新编译**

### 步骤5：在控制台测试网络请求

**打开调试器 → Console 标签，输入：**
```javascript
wx.request({
  url: 'http://127.0.0.1:8080/api/auth/send-code?phone=13800138000',
  method: 'GET',
  success: (res) => {
    console.log('✅ 网络连接成功', res);
  },
  fail: (err) => {
    console.error('❌ 网络连接失败', err);
  }
});
```

**查看结果：**
- ✅ **成功** → 网络正常，问题可能在登录页面代码
- ❌ **失败** → 查看具体错误信息

## 🚨 常见错误及解决方案

### 错误1：`request:fail url not in domain list`

**原因：** 微信开发者工具校验了域名白名单

**解决：**
1. 勾选"不校验合法域名"
2. 清除缓存重新编译

### 错误2：`request:fail connect ECONNREFUSED`

**原因：** 后端服务未启动

**解决：**
```bash
cd seat_server
mvn spring-boot:run
```

### 错误3：`request:fail net::ERR_CONNECTION_REFUSED`

**原因：** 端口被占用或后端未启动

**解决：**
1. 检查后端是否启动
2. 检查端口是否被占用：
   ```bash
   netstat -ano | findstr :8080
   ```

### 错误4：`CORS policy: No 'Access-Control-Allow-Origin'`

**原因：** 后端未配置 CORS

**解决：** 已添加 CORS 配置，重启后端服务

### 错误5：`request:fail timeout`

**原因：** 请求超时

**解决：**
1. 检查后端服务是否正常响应
2. 在浏览器中测试 API 是否正常

## 🔄 重启后端服务（应用CORS配置）

**如果添加了 CORS 配置，需要重启后端：**

1. **停止当前后端服务**（在运行窗口按 `Ctrl + C`）
2. **重新启动：**
   ```bash
   cd seat_server
   mvn spring-boot:run
   ```
3. **等待看到：**
   ```
   Started ZhizuoApplication in X.XXX seconds
   ```

## 📝 验证清单

请按顺序检查：

- [ ] **后端服务正在运行**（8080端口监听）
- [ ] **浏览器能访问** `http://127.0.0.1:8080/doc.html`
- [ ] **微信开发者工具已勾选"不校验合法域名"**
- [ ] **已清除缓存并重新编译**
- [ ] **控制台测试网络请求成功**
- [ ] **后端已重启**（如果添加了CORS配置）

## 💡 如果还是不行

### 方法1：使用局域网IP

如果 `127.0.0.1` 还是不行，使用局域网IP：

1. **获取本机IP：**
   ```bash
   ipconfig
   ```
   找到 IPv4 地址，例如：`192.168.78.196`

2. **修改 `seat_client/utils/request.js` 第7行：**
   ```javascript
   const BASE_URL = 'http://192.168.78.196:8080/api';
   ```

3. **重新编译**

### 方法2：检查防火墙

Windows防火墙可能阻止了连接：

```bash
netsh advfirewall firewall add rule name="Allow 8080" dir=in action=allow protocol=TCP localport=8080
```

### 方法3：查看详细错误信息

1. **打开微信开发者工具调试器**
2. **切换到 Network 标签**
3. **尝试登录**
4. **查看请求详情：**
   - URL 是否正确
   - 状态码是什么
   - 错误信息是什么

## 🎯 快速测试命令

在微信开发者工具控制台执行：

```javascript
// 测试1：简单GET请求
wx.request({
  url: 'http://127.0.0.1:8080/api/auth/send-code?phone=13800138000',
  method: 'GET',
  success: (res) => console.log('✅ 成功', res),
  fail: (err) => console.error('❌ 失败', err)
});

// 测试2：检查BASE_URL
const request = require('./utils/request.js');
console.log('BASE_URL:', request.BASE_URL);
```

## ✅ 修复完成

按照以上步骤操作后，网络连接应该可以正常工作了！

**如果还有问题，请提供：**
1. 微信开发者工具控制台的完整错误信息
2. Network 标签中的请求详情
3. 后端服务的日志输出

