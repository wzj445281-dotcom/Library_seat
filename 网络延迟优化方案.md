# 网络延迟优化方案

## 🎯 问题

真机调试时网络延迟太高，请求太慢。

## ✅ 优化方案

### 方案1：使用微信开发者工具的真机调试代理（推荐⭐）

**这是最快的方案！**

#### 配置步骤：
1. 打开微信开发者工具
2. 点击右上角"详情"
3. 切换到"本地设置"标签
4. **勾选：**
   - ✅ **不校验合法域名**
   - ✅ **使用本地代理**（如果有这个选项）

#### 优势：
- 通过电脑代理，延迟极低
- 不需要修改代码
- 适合开发调试

---

### 方案2：使用带缓存的请求工具（已实现）

**文件：** `seat_client/utils/request-cached.js`

#### 使用方法：

**在需要缓存的页面使用：**
```javascript
// 替换原来的 request
const { request } = require('../../utils/request-cached');

// GET请求会自动缓存
request.get('/app/store/menu').then(res => {
    // 第一次请求会从网络获取
    // 后续请求会从缓存获取（5分钟内）
});
```

#### 缓存策略：
- 菜单数据：缓存5分钟
- 商品详情：缓存10分钟
- 分类数据：缓存30分钟
- 默认：缓存2分钟

#### 优势：
- 减少重复请求
- 网络失败时使用缓存
- 提升用户体验

---

### 方案3：优化请求超时设置

**已实现：** 请求超时设置为10秒

**文件：** `seat_client/utils/request-cached.js`

```javascript
wx.request({
    timeout: 10000, // 10秒超时
    // ...
});
```

---

### 方案4：使用本地存储预加载

**在应用启动时预加载数据：**

```javascript
// app.js
onLaunch() {
    // 预加载菜单数据
    this.preloadMenuData();
}

preloadMenuData() {
    const { request } = require('./utils/request-cached');
    request.get('/app/store/menu').then(res => {
        // 数据已缓存，后续页面加载会很快
    });
}
```

---

### 方案5：批量请求优化

**合并多个请求：**

```javascript
// 使用 Promise.all 并行请求
Promise.all([
    request.get('/app/store/menu'),
    request.get('/app/coupon/list'),
    request.get('/app/favorite/ids')
]).then(([menu, coupons, favorites]) => {
    // 所有数据同时返回
});
```

---

### 方案6：后端优化（需要后端配合）

#### 6.1 启用响应压缩

**文件：** `seat_server/src/main/resources/application.yml`

```yaml
server:
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
    min-response-size: 1024
```

#### 6.2 添加Redis缓存

**在后端添加缓存层：**
```java
@Cacheable(value = "menu", key = "#storeId")
public List<Product> getMenu(Long storeId) {
    // 查询数据库
}
```

#### 6.3 优化数据库查询

- 添加索引
- 减少查询字段
- 使用分页

---

### 方案7：使用HTTP/2（需要HTTPS）

**如果使用HTTPS，可以启用HTTP/2：**
- 多路复用
- 头部压缩
- 服务器推送

---

## 🚀 快速实施

### 步骤1：使用缓存请求工具

**修改菜单页面：**
```javascript
// seat_client/pages/menu/index.js
// 将原来的
const { request } = require('../../utils/request');

// 改为
const { request } = require('../../utils/request-cached');
```

### 步骤2：启用微信开发者工具代理

1. 打开微信开发者工具
2. 详情 → 本地设置
3. 勾选"不校验合法域名"
4. 如果有"使用本地代理"选项，也勾选

### 步骤3：测试效果

1. 清除缓存
2. 重新编译
3. 真机调试
4. 观察请求速度

---

## 📊 性能对比

### 优化前：
- 每次请求：200-500ms
- 重复请求：每次都请求网络
- 网络失败：直接报错

### 优化后：
- 首次请求：200-500ms
- 缓存命中：<10ms（从本地读取）
- 网络失败：使用缓存数据

---

## ⚠️ 注意事项

1. **缓存数据可能不是最新的**
   - 重要数据（如订单状态）不要使用缓存
   - 可以手动清除缓存：`request.clearCache()`

2. **缓存占用存储空间**
   - 定期清理过期缓存
   - 控制缓存大小

3. **真机调试代理**
   - 只在开发环境使用
   - 生产环境需要配置合法域名

---

## 🎯 推荐方案组合

### 开发环境：
1. ✅ 使用微信开发者工具代理（最快）
2. ✅ 使用缓存请求工具（备用）

### 生产环境：
1. ✅ 使用缓存请求工具
2. ✅ 后端启用响应压缩
3. ✅ 后端添加Redis缓存
4. ✅ 优化数据库查询

---

## 📝 相关文件

- `seat_client/utils/request-cached.js` - 带缓存的请求工具
- `seat_client/utils/request.js` - 原始请求工具
- `seat_server/src/main/resources/application.yml` - 后端配置

---

## 🔧 如果还是慢

1. **检查网络质量**
   - WiFi信号强度
   - 路由器性能
   - 网络带宽

2. **检查后端性能**
   - 数据库查询时间
   - 接口响应时间
   - 服务器负载

3. **使用性能分析工具**
   - 微信开发者工具 → 性能面板
   - 查看请求耗时
   - 找出瓶颈

