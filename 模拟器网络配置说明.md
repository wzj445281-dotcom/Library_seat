# 模拟器网络配置说明

## 🔍 问题分析

**现象：**
- ✅ 真机可以连接网络
- ❌ 微信开发者工具模拟器无法连接网络

**原因：**
微信开发者工具的模拟器可能不支持 `localhost` 或 `127.0.0.1`，需要使用**局域网IP**。

## ✅ 已完成的修复

### 修改 BASE_URL 为局域网IP

**修改前：**
```javascript
const BASE_URL = 'http://127.0.0.1:8080/api';
```

**修改后：**
```javascript
const BASE_URL = 'http://192.168.78.196:8080/api';
```

**说明：** 使用局域网IP后，模拟器和真机都可以正常连接。

## 🔧 配置步骤

### 步骤1：确认你的局域网IP

**Windows：**
```bash
ipconfig
```
找到连接WiFi的网卡的 IPv4 地址，例如：`192.168.78.196`

**Mac/Linux：**
```bash
ifconfig
```

### 步骤2：修改 BASE_URL

如果IP地址不是 `192.168.78.196`，请修改 `seat_client/utils/request.js` 第7行：

```javascript
const BASE_URL = 'http://你的IP:8080/api';
```

例如：
```javascript
const BASE_URL = 'http://192.168.1.100:8080/api';
```

### 步骤3：配置微信开发者工具

1. **打开微信开发者工具**
2. **点击右上角"详情"**
3. **切换到"本地设置"标签**
4. **勾选：**
   - ✅ **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**
   - ✅ **不校验安全域名（TLS版本）**

### 步骤4：清除缓存重新编译

1. **点击"编译"旁边的下拉箭头**
2. **选择"清除缓存"**
3. **重新编译**

## 🎯 验证

### 在模拟器中测试

1. **打开登录页面**
2. **输入手机号和密码**
3. **点击"登录"**
4. **应该能正常连接**

### 在控制台测试

打开调试器 → Console 标签，输入：

```javascript
wx.request({
  url: 'http://192.168.78.196:8080/api/auth/send-code?phone=13800138000',
  method: 'GET',
  success: (res) => {
    console.log('✅ 模拟器网络连接成功！', res);
  },
  fail: (err) => {
    console.error('❌ 模拟器网络连接失败', err);
  }
});
```

## 📝 注意事项

### 1. IP地址可能变化

如果电脑的IP地址变化了（例如切换WiFi），需要重新修改 BASE_URL。

**解决方案：**
- 在路由器中设置静态IP
- 或使用DHCP保留IP

### 2. 确保电脑和手机在同一WiFi

- ✅ **模拟器**：使用局域网IP，可以连接
- ✅ **真机**：使用局域网IP，可以连接
- ❌ **如果不在同一WiFi**：无法连接

### 3. 防火墙设置

确保Windows防火墙允许8080端口：

```bash
netsh advfirewall firewall add rule name="Allow 8080" dir=in action=allow protocol=TCP localport=8080
```

## 🔄 如果IP地址变化

如果IP地址变化了，需要：

1. **获取新IP：**
   ```bash
   ipconfig
   ```

2. **修改 `seat_client/utils/request.js` 第7行：**
   ```javascript
   const BASE_URL = 'http://新IP:8080/api';
   ```

3. **重新编译**

## ✅ 现在应该可以了

使用局域网IP后，模拟器和真机都应该可以正常连接网络了！

如果还有问题，请检查：
1. IP地址是否正确
2. 后端服务是否在运行
3. 微信开发者工具是否勾选了"不校验合法域名"

