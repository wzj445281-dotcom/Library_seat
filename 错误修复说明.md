# 错误修复说明

## ✅ 已修复的问题

### 1. qrcode.js 模块找不到

**问题：** `Error: module 'utils/qrcode.js' is not defined`

**原因：** 文件不存在，只有 `QRCODE.js`（大写）

**修复：** 创建了 `seat_client/utils/qrcode.js` 包装文件，适配小程序 canvas API

### 2. API路径重复 /api

**问题：** `GET http://127.0.0.1:8080/api/api/app/store/menu` 404错误

**原因：** BASE_URL 已经是 `http://192.168.78.196:8080/api`，但API路径又以 `/api` 开头，导致重复

**修复：** 移除了所有API路径中的 `/api` 前缀

**修改的文件：**
- ✅ `seat_client/api/product.js` - `/api/app/store/menu` → `/app/store/menu`
- ✅ `seat_client/api/order.js` - `/api/app/store/order/*` → `/app/store/order/*`
- ✅ `seat_client/api/address.js` - `/api/app/address/*` → `/app/address/*`
- ✅ `seat_client/api/coupon.js` - `/api/app/coupon/*` → `/app/coupon/*`
- ✅ `seat_client/api/user.js` - `/api/app/user/*` → `/app/user/*`
- ✅ `seat_client/pages/login/login.js` - `/api/auth/*` → `/auth/*`
- ✅ `seat_client/pages/menu/index.js` - `/api/app/favorite/*` → `/app/favorite/*`
- ✅ `seat_client/pages/orders/list.js` - `/api/app/store/order/*` → `/app/store/order/*`
- ✅ `seat_client/pages/coupon/index.js` - `/api/app/coupon/*` → `/app/coupon/*`
- ✅ `seat_client/pages/feedback/feedback.js` - `/api/app/feedback/*` → `/app/feedback/*`
- ✅ `seat_client/pages/ai_chat/ai_chat.js` - `/api/app/ai/*` → `/app/ai/*`

## 📝 API路径规则

**BASE_URL：** `http://192.168.78.196:8080/api`

**API路径格式：**
- ❌ 错误：`/api/app/store/menu` → `http://192.168.78.196:8080/api/api/app/store/menu`
- ✅ 正确：`/app/store/menu` → `http://192.168.78.196:8080/api/app/store/menu`

## 🔍 验证修复

### 1. 清除缓存重新编译

在微信开发者工具中：
1. 点击"编译"旁边的下拉箭头
2. 选择"清除缓存"
3. 重新编译

### 2. 测试功能

- ✅ 登录功能
- ✅ 菜单加载
- ✅ 订单列表
- ✅ 订单详情（二维码）
- ✅ 优惠券
- ✅ 地址管理
- ✅ AI聊天
- ✅ 反馈提交

## ⚠️ 注意事项

### 如果还有404错误

检查：
1. 后端服务是否运行
2. API路径是否正确（不应以 `/api` 开头）
3. BASE_URL 是否正确

### 如果二维码还是有问题

检查：
1. `seat_client/utils/qrcode.js` 文件是否存在
2. `seat_client/pages/orders/detail/detail.js` 中的引用是否正确

## 🎉 修复完成

现在所有API路径都已修复，应该不会再出现404错误了！

