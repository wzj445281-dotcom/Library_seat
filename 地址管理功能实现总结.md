# 地址管理功能实现总结

## ✅ 已完成功能

### 1. 数据库层 (SQL)

**文件**: `seat_server/create_user_address_table.sql`

```sql
CREATE TABLE `user_address` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL COMMENT '用户ID',
  `name` varchar(50) NOT NULL COMMENT '收货人姓名',
  `phone` varchar(20) NOT NULL COMMENT '收货人电话',
  `province` varchar(50) DEFAULT NULL COMMENT '省份',
  `city` varchar(50) DEFAULT NULL COMMENT '城市',
  `district` varchar(50) DEFAULT NULL COMMENT '区县',
  `detail` varchar(255) NOT NULL COMMENT '详细地址',
  `is_default` tinyint(1) DEFAULT 0 COMMENT '是否默认地址 0:否 1:是',
  `latitude` decimal(10,7) DEFAULT NULL COMMENT '纬度',
  `longitude` decimal(10,7) DEFAULT NULL COMMENT '经度',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_is_default` (`user_id`, `is_default`)
) ENGINE=InnoDB COMMENT='用户收货地址表';
```

**字段说明**:
- ✅ 包含所有必需字段：id, user_id, name, phone, province, city, district, detail, is_default
- ✅ 额外字段：latitude, longitude（用于地图定位）
- ✅ 索引优化：user_id 和 (user_id, is_default) 联合索引

---

### 2. 后端实现 (Java)

#### 2.1 Entity 实体类

**文件**: `seat_server/src/main/java/com/example/zhizuo/core/entity/UserAddress.java`

- ✅ 使用 MyBatis Plus `@TableName` 注解
- ✅ 使用 Lombok `@Data` 自动生成 getter/setter
- ✅ 包含所有字段的完整定义
- ✅ 提供 `getFullAddress()` 方法获取完整地址字符串

#### 2.2 Mapper 接口

**文件**: `seat_server/src/main/java/com/example/zhizuo/core/mapper/UserAddressMapper.java`

- ✅ 继承 `BaseMapper<UserAddress>`，自动提供 CRUD 方法
- ✅ 使用 `@Mapper` 注解

#### 2.3 Service 服务层

**文件**: `seat_server/src/main/java/com/example/zhizuo/core/service/UserAddressService.java` (接口)
**文件**: `seat_server/src/main/java/com/example/zhizuo/core/service/impl/UserAddressServiceImpl.java` (实现)

**核心功能**:
- ✅ `getUserAddresses(Long userId)`: 获取用户地址列表（默认地址优先）
- ✅ `addAddress(Long userId, UserAddress address)`: 添加地址（自动处理默认地址逻辑）
- ✅ `updateAddress(Long userId, Long addressId, UserAddress address)`: 更新地址（验证归属权）
- ✅ `deleteAddress(Long userId, Long addressId)`: 删除地址（验证归属权）
- ✅ `setDefaultAddress(Long userId, Long addressId)`: 设置默认地址（自动取消其他默认地址）
- ✅ `getDefaultAddress(Long userId)`: 获取默认地址

**关键特性**:
- ✅ 事务管理：使用 `@Transactional` 确保数据一致性
- ✅ 默认地址逻辑：设置新默认地址时，自动取消其他默认地址
- ✅ 权限验证：更新/删除时验证地址归属权

#### 2.4 Controller 控制器

**文件**: `seat_server/src/main/java/com/example/zhizuo/api/app/UserAddressController.java`

**API 接口**:
- ✅ `GET /api/app/address/list`: 获取当前用户地址列表
- ✅ `GET /api/app/address/default`: 获取默认地址
- ✅ `POST /api/app/address/add`: 新增地址
- ✅ `PUT /api/app/address/update/{id}`: 修改地址
- ✅ `DELETE /api/app/address/delete/{id}`: 删除地址
- ✅ `PUT /api/app/address/set-default/{id}`: 设置默认地址

**安全特性**:
- ✅ 使用 `SecurityUtils.getUserId()` 自动获取当前登录用户ID
- ✅ 所有操作都基于当前登录用户，防止越权访问

---

### 3. 前端实现 (微信小程序)

#### 3.1 API 封装

**文件**: `seat_client/api/address.js`

**API 方法**:
- ✅ `getAddressList()`: 获取地址列表
- ✅ `getDefaultAddress()`: 获取默认地址
- ✅ `addAddress(data)`: 添加地址
- ✅ `updateAddress(id, data)`: 更新地址
- ✅ `deleteAddress(id)`: 删除地址
- ✅ `setDefaultAddress(id)`: 设置默认地址

**路径配置**: 所有请求路径为 `/app/address/*`，会自动拼接 `BASE_URL`（如 `http://localhost:8080/api`）

#### 3.2 地址列表页面

**文件**: 
- `seat_client/pages/address/list.js` (逻辑)
- `seat_client/pages/address/list.wxml` (视图)
- `seat_client/pages/address/list.wxss` (样式)

**功能**:
- ✅ 显示地址列表（默认地址优先显示）
- ✅ 支持选择模式（从结算页跳转时）
- ✅ 编辑地址
- ✅ 删除地址（带确认提示）
- ✅ 设置默认地址
- ✅ 添加新地址

#### 3.3 地址编辑页面

**文件**: 
- `seat_client/pages/address/edit.js` (逻辑)
- `seat_client/pages/address/edit.wxml` (视图)
- `seat_client/pages/address/edit.wxss` (样式)

**功能**:
- ✅ 表单数据绑定（name, phone, province, city, district, detail, isDefault）
- ✅ 使用 `wx.chooseLocation` 选择地理位置
- ✅ 自动解析省市区信息（从微信返回的地址字符串）
- ✅ 数据校验：
  - 必填字段：name, phone, detail
  - 手机号格式校验：`/^1[3-9]\d{9}$/`
- ✅ 保存/更新地址（根据是否有 id 判断）
- ✅ 保存成功后自动返回上一页

**核心代码片段**:

```javascript
// 选择位置（使用微信地图）
chooseLocation() {
  wx.chooseLocation({
    success: (res) => {
      // 设置详细地址和经纬度
      this.setData({
        detail: res.address || res.name,
        latitude: res.latitude,
        longitude: res.longitude
      });
      
      // 自动解析省市区信息
      const address = res.address || '';
      // ... 解析逻辑
    }
  });
}
```

---

## 🔧 使用说明

### 1. 数据库初始化

执行 SQL 脚本：
```bash
mysql -u root -p < seat_server/create_user_address_table.sql
```

或直接在 MySQL 客户端执行 `seat_server/create_user_address_table.sql` 文件内容。

### 2. 后端配置

确保 `SecurityUtils.getUserId()` 能正确获取当前登录用户ID。

### 3. 前端配置

确保 `seat_client/utils/request.js` 中的 `BASE_URL` 配置正确：
- 本地开发：`http://localhost:8080/api`
- 真机调试：`http://你的局域网IP:8080/api`
- Docker部署：`http://你的IP:8081/api`

### 4. 页面路由

在 `app.json` 中已配置：
```json
{
  "pages": [
    "pages/address/list",
    "pages/address/edit"
  ]
}
```

---

## 🎨 UI 风格

- ✅ 采用瑞幸蓝主题色 `#0022AB`
- ✅ 与其他页面保持一致的样式风格
- ✅ 响应式布局，适配不同屏幕尺寸

---

## 🔒 安全特性

1. **权限控制**: 所有操作都基于当前登录用户，防止越权访问
2. **数据验证**: 前端和后端都进行数据校验
3. **事务管理**: 关键操作使用事务确保数据一致性

---

## 📝 注意事项

1. **位置权限**: 使用 `wx.chooseLocation` 需要用户授权位置权限
2. **地址解析**: 当前使用简单的字符串匹配解析省市区，可根据实际需求集成第三方地址解析服务
3. **默认地址**: 每个用户只能有一个默认地址，设置新默认地址时会自动取消其他默认地址

---

## ✅ 功能完整性检查

- [x] SQL 表结构完整
- [x] Entity 实体类完整
- [x] Mapper 接口完整
- [x] Service 服务层完整
- [x] Controller 控制器完整
- [x] 前端 API 封装完整
- [x] 地址列表页面完整
- [x] 地址编辑页面完整
- [x] 地图定位功能完整
- [x] 数据校验完整
- [x] 错误处理完整

---

## 🎉 总结

地址管理功能已完整实现，包括：
- ✅ 完整的后端 API（6个接口）
- ✅ 完整的数据库表结构
- ✅ 完整的前端页面（列表页 + 编辑页）
- ✅ 地图定位功能
- ✅ 数据校验和错误处理
- ✅ 安全权限控制

现在可以在结算页面中调用地址选择功能，完成外卖下单流程的闭环。

