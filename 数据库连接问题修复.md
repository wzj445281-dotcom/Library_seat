# 数据库连接问题修复指南

## 🔴 错误信息

```
Unable to create requested service [org.hibernate.engine.jdbc.env.spi.JdbcEnvironment]
Access to DialectResolutionInfo cannot be null when 'hibernate.dialect' not set
```

## ✅ 问题原因

这个错误通常是因为：
1. **数据库不存在** - 配置中使用的数据库 `ruixing` 可能不存在
2. **MySQL 服务未启动** - MySQL 服务没有运行
3. **用户名/密码错误** - 数据库连接凭据不正确
4. **端口错误** - MySQL 不在 3306 端口运行

## 🔧 解决方案

### 方案1：使用已存在的数据库（推荐）

我已经将配置改为使用 `zhizuo` 数据库（如果已存在）。

**检查数据库是否存在：**
```bash
mysql -uroot -p000000 -e "SHOW DATABASES;"
```

**如果 `zhizuo` 数据库存在，配置已自动修改，直接重启后端即可。**

### 方案2：创建 ruixing 数据库

如果希望使用 `ruixing` 数据库，执行：

```sql
CREATE DATABASE ruixing CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

然后修改 `application.yml` 第18行，改回：
```yaml
url: jdbc:mysql://localhost:3306/ruixing?...
```

### 方案3：检查 MySQL 服务

**Windows：**
```bash
# 检查 MySQL 服务状态
Get-Service | Where-Object {$_.Name -like "*mysql*"}

# 启动 MySQL 服务
net start MySQL
# 或
Start-Service MySQL
```

**如果 MySQL 未安装：**
- 下载 MySQL 8.0 安装包
- 或使用 XAMPP/WAMP 等集成环境

### 方案4：修改数据库密码

如果 MySQL 密码不是 `000000`，修改 `application.yml` 第20行：

```yaml
password: ${SPRING_DATASOURCE_PASSWORD:你的密码}
```

## 📝 快速修复步骤

1. **检查数据库是否存在：**
   ```bash
   mysql -uroot -p000000 -e "SHOW DATABASES;"
   ```

2. **如果 `zhizuo` 存在：**
   - 配置已自动修改为使用 `zhizuo`
   - 直接重启后端即可

3. **如果 `zhizuo` 不存在，但 `ruixing` 存在：**
   - 修改 `application.yml` 第18行，将 `zhizuo` 改为 `ruixing`

4. **如果两个都不存在：**
   - 创建数据库：
     ```sql
     CREATE DATABASE zhizuo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
     ```
   - 执行 SQL 初始化脚本（在 `seat_server/` 目录下）

5. **重启后端：**
   ```bash
   cd seat_server
   mvn spring-boot:run
   ```

## ✅ 验证修复

启动后端后，应该看到：
```
Started ZhizuoApplication in X.XXX seconds
```

而不是数据库连接错误。

## 🔍 其他可能的问题

### Redis 连接失败

如果 Redis 未安装，可以暂时注释掉 Redis 配置，或安装 Redis：
- Windows: 下载 Redis for Windows
- Mac: `brew install redis`
- Linux: `sudo apt-get install redis-server`

### 端口被占用

如果 8080 端口被占用，修改 `application.yml` 第2行：
```yaml
server:
  port: 8081  # 改为其他端口
```

记得同步修改前端的 `BASE_URL`。

