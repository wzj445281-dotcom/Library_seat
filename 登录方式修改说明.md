# 登录方式修改说明

## ✅ 已完成的修改

已将登录方式从**验证码登录**改为**手机号+密码登录**。

## 📝 修改内容

### 1. 前端修改

#### `seat_client/pages/login/login.wxml`
- ❌ 移除了验证码输入框和"获取验证码"按钮
- ✅ 添加了密码输入框（`type="password"`）
- ✅ 注册模式下提示"请设置密码（至少6位）"
- ✅ 登录模式下提示"请输入密码"

#### `seat_client/pages/login/login.js`
- ❌ 移除了 `sendCode()` 方法
- ❌ 移除了 `code`、`codeText`、`countdown` 字段
- ✅ 添加了 `password` 字段
- ✅ 修改 `handleLogin()` 方法：
  - 验证密码而不是验证码
  - 密码长度至少6位
  - 传递 `{ phone, password }` 到后端
- ✅ 修改 `handleRegister()` 方法：
  - 注册时要求输入密码
  - 验证密码长度至少6位
  - 传递 `{ phone, name, password }` 到后端

### 2. 后端修改

#### `seat_server/src/main/java/com/example/zhizuo/core/dto/UserLoginDTO.java`
- ❌ 移除了 `code` 字段
- ✅ 添加了 `password` 字段

#### `seat_server/src/main/java/com/example/zhizuo/core/dto/UserRegisterDTO.java`
- ❌ 移除了 `code` 字段
- ✅ 添加了 `password` 字段

#### `seat_server/src/main/java/com/example/zhizuo/api/app/AuthController.java`
- ✅ 修改 `login()` 方法：
  - 移除了验证码验证逻辑
  - 改为验证密码（使用 `passwordEncoder.matches()`）
  - 移除了 Redis 验证码相关代码
- ✅ 修改 `register()` 方法：
  - 移除了验证码验证逻辑
  - 添加了密码长度验证（至少6位）
  - 使用 `passwordEncoder.encode()` 加密存储密码
  - 移除了 Redis 验证码相关代码
- ⚠️ **保留**了 `sendCode()` 方法（虽然不再使用，但保留以防需要）

## 🎯 使用方式

### 登录流程

1. **输入手机号**（例如：13800138000）
2. **输入密码**（至少6位）
3. **点击"登录"**
4. 后端验证密码后返回 Token

### 注册流程

1. **输入手机号**（例如：13800138000）
2. **输入姓名**（例如：张三）
3. **设置密码**（至少6位）
4. **点击"注册"**
5. 后端创建用户并加密存储密码

## 🔒 安全性说明

1. **密码加密**：使用 Spring Security 的 `BCryptPasswordEncoder` 加密存储
2. **密码验证**：使用 `passwordEncoder.matches()` 验证密码
3. **密码长度**：前端和后端都验证密码至少6位

## ⚠️ 注意事项

### 现有用户数据

如果数据库中已有使用验证码注册的用户，他们的密码可能是：
- 加密后的手机号（旧逻辑：`passwordEncoder.encode(phone)`）
- 或其他默认值

**建议：**
1. 让这些用户通过"忘记密码"功能重置密码
2. 或手动更新数据库中的密码

### 测试账号

如果需要测试，可以：
1. **注册新账号**：使用手机号+密码注册
2. **或手动创建测试用户**：
   ```sql
   -- 密码是 "123456" 的加密值
   UPDATE user SET password = '$2a$10$...' WHERE phone = '13800138000';
   ```

## 🚀 测试步骤

### 1. 测试注册

1. 打开登录页面
2. 点击"没有账号？去注册"
3. 输入：
   - 手机号：`13800138000`
   - 姓名：`测试用户`
   - 密码：`123456`
4. 点击"注册"
5. 应该看到"注册成功，请登录"

### 2. 测试登录

1. 在登录页面输入：
   - 手机号：`13800138000`
   - 密码：`123456`
2. 点击"登录"
3. 应该看到"登录成功"并跳转到首页

### 3. 测试错误情况

1. **密码错误**：应该提示"密码错误"
2. **用户不存在**：应该提示"用户不存在，请先注册"
3. **密码太短**：应该提示"密码至少6位"

## ✅ 验证清单

- [x] 前端登录页面显示密码输入框
- [x] 前端注册页面显示密码输入框
- [x] 前端登录逻辑传递密码
- [x] 前端注册逻辑传递密码
- [x] 后端DTO包含password字段
- [x] 后端登录接口验证密码
- [x] 后端注册接口保存密码
- [x] 密码加密存储
- [x] 密码长度验证

## 📝 后续优化建议

1. **忘记密码功能**：添加找回密码功能
2. **密码强度验证**：要求包含数字、字母等
3. **登录失败次数限制**：防止暴力破解
4. **记住密码**：可选功能（本地存储）

## 🎉 完成！

现在系统已完全切换到手机号+密码登录方式，不再依赖验证码和Redis。

