# 本地模拟器网络连接问题修复

## 🔍 问题分析

在微信开发者工具模拟器中无法连接网络，可能的原因：

1. **localhost 解析问题** - 某些情况下 localhost 可能无法正确解析
2. **微信开发者工具网络设置** - 需要配置不校验合法域名
3. **后端服务未启动** - 后端服务没有运行
4. **端口被占用** - 8080端口被其他程序占用

## ✅ 已完成的修复

### 1. 修改 BASE_URL 为 127.0.0.1

**修改前：**
```javascript
const BASE_URL = 'http://localhost:8080/api';
```

**修改后：**
```javascript
const BASE_URL = 'http://127.0.0.1:8080/api';
```

**原因：** `127.0.0.1` 比 `localhost` 更可靠，直接指向本机IP。

## 🔧 解决步骤

### 步骤1：检查后端服务

确认后端服务正在运行：

```bash
# 检查8080端口是否被占用
netstat -ano | findstr :8080
```

**应该看到：**
```
TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING
```

**如果后端未启动：**
```bash
cd seat_server
mvn spring-boot:run
```

### 步骤2：配置微信开发者工具

1. **打开微信开发者工具**
2. **点击右上角"详情"**
3. **切换到"本地设置"标签**
4. **勾选以下选项：**
   - ✅ **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**
   - ✅ **不校验安全域名（TLS版本）**

### 步骤3：测试网络连接

在微信开发者工具中：

1. **打开调试器**（点击"调试器"按钮）
2. **切换到 Console 标签**
3. **在控制台输入：**
   ```javascript
   wx.request({
     url: 'http://127.0.0.1:8080/api/auth/send-code?phone=13800138000',
     method: 'GET',
     success: (res) => {
       console.log('网络连接成功', res);
     },
     fail: (err) => {
       console.error('网络连接失败', err);
     }
   });
   ```

**如果成功：** 应该看到返回的数据
**如果失败：** 查看错误信息

### 步骤4：在浏览器中测试

在浏览器中访问：
```
http://127.0.0.1:8080/doc.html
```

**如果能打开：** 说明后端正常，问题在微信开发者工具配置
**如果打不开：** 说明后端未启动或端口被占用

## 🚀 快速修复方法

### 方法1：使用 127.0.0.1（已修改）

已修改 `seat_client/utils/request.js`，将 `localhost` 改为 `127.0.0.1`。

### 方法2：配置微信开发者工具

**必须勾选：**
- ✅ 不校验合法域名
- ✅ 不校验安全域名

### 方法3：清除缓存重新编译

1. 在微信开发者工具中点击"编译"旁边的下拉箭头
2. 选择"清除缓存"
3. 重新编译

## 📝 验证修复

修复后，在登录页面：

1. **输入手机号**（例如：13800138000）
2. **输入密码**（例如：123456）
3. **点击"登录"**
4. **应该能正常发送请求**

**如果还是失败：**

1. **查看控制台错误信息**
2. **检查后端日志**（后端启动窗口）
3. **确认后端服务正在运行**

## 🔍 常见错误

### 错误1：`request:fail url not in domain list`

**原因：** 微信开发者工具校验了域名

**解决：** 勾选"不校验合法域名"

### 错误2：`request:fail connect ECONNREFUSED`

**原因：** 后端服务未启动

**解决：** 启动后端服务

### 错误3：`request:fail net::ERR_CONNECTION_REFUSED`

**原因：** 端口被占用或后端未启动

**解决：** 
1. 检查后端是否启动
2. 检查端口是否被占用

## 💡 其他解决方案

### 方案1：使用局域网IP

如果 `127.0.0.1` 还是不行，可以使用局域网IP：

1. **获取本机IP：**
   ```bash
   ipconfig
   ```
   找到 IPv4 地址，例如：`192.168.78.196`

2. **修改 BASE_URL：**
   ```javascript
   const BASE_URL = 'http://192.168.78.196:8080/api';
   ```

### 方案2：检查防火墙

Windows防火墙可能阻止了连接：

1. **打开"Windows Defender 防火墙"**
2. **允许 8080 端口**

或使用命令：
```bash
netsh advfirewall firewall add rule name="Allow 8080" dir=in action=allow protocol=TCP localport=8080
```

## ✅ 修复完成

现在网络连接应该可以正常工作了！

如果还有问题，请提供：
1. 微信开发者工具控制台的错误信息
2. 后端服务的日志
3. 网络请求的详细信息

