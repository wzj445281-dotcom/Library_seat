# 后端启动状态检查

## ✅ 后端已启动

后端服务已在后台启动，请按以下步骤验证：

### 1. 检查端口（已执行）
```bash
netstat -ano | findstr :8080
```

### 2. 访问测试

**在浏览器中打开以下地址：**

- **API 文档：** http://localhost:8080/doc.html
- **后台管理：** http://localhost:8080/admin.html
- **商品管理：** http://localhost:8080/product_manage.html

### 3. 如果无法访问

**可能的原因：**
1. 后端还在启动中（首次启动需要 1-2 分钟）
2. 数据库连接失败
3. Redis 连接失败

**检查方法：**
1. 查看后端日志（在启动的终端窗口）
2. 检查 MySQL 是否运行：`net start MySQL`（Windows）
3. 检查 Redis 是否运行：`redis-cli ping`（应该返回 PONG）

### 4. 获取本机 IP（用于前端配置）

**Windows：**
```bash
ipconfig
# 找到 "IPv4 地址"，例如：192.168.1.100
```

**修改前端配置：**
编辑 `seat_client/utils/request.js`，将：
```javascript
const BASE_URL = 'http://localhost:8080/api';
```
改为：
```javascript
const BASE_URL = 'http://192.168.1.100:8080/api';  // 使用你的IP
```

### 5. 启动前端

1. 打开微信开发者工具
2. 导入项目：选择 `seat_client` 文件夹
3. 点击"编译"

---

## 📝 快速命令

**停止后端：**
- 在启动的终端窗口按 `Ctrl+C`
- 或查找 Java 进程并结束：`taskkill /F /IM java.exe`

**重启后端：**
```bash
cd seat_server
mvn spring-boot:run
```

