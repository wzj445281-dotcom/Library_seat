# 瑞幸咖啡小程序 - 完整演示步骤指南

## 📋 演示前准备检查清单

### ✅ 必需环境
- [ ] Docker Desktop 已安装并运行
- [ ] 微信开发者工具已安装
- [ ] DeepSeek API Key（可选，不配置将使用Mock模式）

### ✅ 项目文件检查
- [ ] 已克隆项目代码
- [ ] `docker-compose.yml` 文件存在
- [ ] 所有 SQL 初始化脚本存在

---

## 🚀 第一步：环境配置（5分钟）

### 1.1 配置 DeepSeek API Key（可选）

**如果不配置，AI功能将使用Mock模式，仍可演示，但推荐配置以获得完整体验。**

编辑 `docker-compose.yml` 文件，找到第9行：

```yaml
environment:
  - DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx # 【必填】记得填 Key
```

替换为你的真实 API Key：
```yaml
environment:
  - DEEPSEEK_API_KEY=sk-your-actual-key-here
```

**获取 API Key：**
1. 访问 https://platform.deepseek.com/
2. 注册/登录账号
3. 在 API Keys 页面创建新的 Key
4. 复制 Key 并填入配置

---

## 🐳 第二步：启动后端服务（10分钟）

### 2.1 使用 Docker Compose 一键启动

在项目根目录执行：

```bash
docker-compose up --build -d
```

**等待时间：** 首次构建需要 3-5 分钟，后续启动约 1-2 分钟

### 2.2 检查服务状态

```bash
docker-compose ps
```

**预期结果：** 所有服务状态应为 `Up` 或 `Started`

应该看到以下服务：
- ✅ `zhizuo-ai` (AI服务)
- ✅ `zhizuo-db` (MySQL数据库)
- ✅ `zhizuo-redis` (Redis缓存)
- ✅ `zhizuo-rabbitmq` (RabbitMQ消息队列)
- ✅ `zhizuo-server` (Java后端服务)

### 2.3 查看服务日志（可选）

如果服务启动失败，查看日志：

```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f seat-server
docker-compose logs -f mysql
```

### 2.4 初始化数据库

**重要：** 数据库容器启动后，需要手动执行 SQL 脚本初始化数据。

#### 方式一：使用 Docker Exec 执行

```bash
# 进入 MySQL 容器
docker exec -it zhizuo-db mysql -uroot -proot

# 在 MySQL 命令行中执行
USE zhizuo_db;

# 依次执行以下 SQL 脚本（按顺序）
SOURCE /path/to/seat_server/init.sql;
SOURCE /path/to/seat_server/create_user_table.sql;
SOURCE /path/to/seat_server/create_user_address_table.sql;
SOURCE /path/to/seat_server/create_user_favorite_table.sql;
SOURCE /path/to/seat_server/coupon_schema.sql;
SOURCE /path/to/seat_server/alter_orders_table.sql;
```

#### 方式二：使用 Navicat/DBeaver 连接

**连接信息：**
- Host: `localhost`
- Port: `3307`
- Username: `root`
- Password: `root`
- Database: `zhizuo_db`

**执行顺序：**
1. `seat_server/init.sql` - 创建商品、订单等核心表
2. `seat_server/create_user_table.sql` - 创建用户表
3. `seat_server/create_user_address_table.sql` - 创建地址表
4. `seat_server/create_user_favorite_table.sql` - 创建收藏表
5. `seat_server/coupon_schema.sql` - 创建优惠券表
6. `seat_server/alter_orders_table.sql` - 修改订单表结构

### 2.5 验证后端服务

打开浏览器访问：

- **API 文档：** http://localhost:8081/doc.html (Knife4j)
- **后台管理：** http://localhost:8081/admin.html
- **商品管理：** http://localhost:8081/product_manage.html
- **AI监控大屏：** http://localhost:8081/monitor.html

**如果页面正常显示，说明后端启动成功！**

---

## 📱 第三步：配置微信小程序前端（5分钟）

### 3.1 打开微信开发者工具

1. 打开微信开发者工具
2. 选择"导入项目"
3. 选择项目目录：`seat_client` 文件夹

### 3.2 配置后端 API 地址

**重要：** 微信小程序无法使用 `localhost`，必须使用局域网 IP 或服务器 IP。

#### 获取本机 IP 地址

**Windows：**
```bash
ipconfig
# 找到 "IPv4 地址"，例如：192.168.1.100
```

**Mac/Linux：**
```bash
ifconfig
# 找到 "inet"，例如：192.168.1.100
```

#### 修改前端配置

编辑 `seat_client/utils/request.js` 文件，第7行：

```javascript
// 修改前
const BASE_URL = 'http://localhost:8080/api';

// 修改后（使用你的IP和Docker映射端口）
const BASE_URL = 'http://192.168.1.100:8081/api';
```

**注意：**
- 如果使用 Docker，端口是 `8081`
- 如果本地运行后端，端口是 `8080`
- IP 地址必须是局域网 IP，不能是 `localhost` 或 `127.0.0.1`

### 3.3 配置小程序 AppID（可选）

如果使用测试号，可以跳过。如果需要正式发布，需要：

1. 在微信公众平台注册小程序
2. 获取 AppID
3. 在 `seat_client/app.json` 或项目设置中配置 AppID

---

## 🎬 第四步：功能演示流程（20分钟）

### 4.1 登录注册功能演示

**演示步骤：**

1. **打开小程序** → 自动跳转到登录页（如果未登录）
2. **发送验证码**
   - 输入手机号（例如：`13800138000`）
   - 点击"获取验证码"
   - 查看后端日志或数据库 `user` 表，验证码存储在 Redis 中
   - 等待60秒倒计时
3. **注册新用户**
   - 输入姓名（例如：`张三`）
   - 输入验证码（从 Redis 或日志中获取，或使用测试验证码：`123456`）
   - 点击"注册"
   - 成功后自动跳转到菜单页
4. **登录已有用户**
   - 输入已注册的手机号
   - 输入验证码
   - 点击"登录"

**验证点：**
- ✅ 验证码发送成功（60秒倒计时）
- ✅ 注册成功，跳转到菜单页
- ✅ Token 已保存到本地存储

---

### 4.2 商品菜单浏览功能演示

**演示步骤：**

1. **浏览商品分类**
   - 左侧显示分类列表（☕️ 畅销榜单、💻 编程技术等）
   - 点击分类，右侧商品列表更新
2. **查看商品详情**
   - 点击商品卡片
   - 查看商品图片、价格、描述
3. **收藏功能**
   - 点击商品右上角的心形图标
   - 图标变为红色（已收藏）
   - 再次点击取消收藏
4. **添加到购物车**
   - 点击商品卡片上的"+"按钮
   - 商品数量增加
   - 底部购物车图标显示数量

**验证点：**
- ✅ 商品列表正常加载
- ✅ 分类切换正常
- ✅ 收藏状态实时更新
- ✅ 购物车数量正确显示

---

### 4.3 地址管理功能演示

**演示步骤：**

1. **进入地址管理**
   - 点击"我的" → "地址管理"
   - 或从结算页进入
2. **新增地址**
   - 点击"新增地址"
   - 填写收货人信息（姓名、电话）
   - 点击"选择地址" → 使用微信地图选择位置
   - 填写详细地址
   - 选择"设为默认地址"
   - 保存
3. **编辑地址**
   - 点击已有地址
   - 修改信息
   - 保存
4. **删除地址**
   - 长按地址卡片
   - 确认删除
5. **设置默认地址**
   - 点击地址卡片上的"设为默认"按钮

**验证点：**
- ✅ 地址列表正常显示
- ✅ 地图定位功能正常
- ✅ 默认地址标记正确
- ✅ 地址选择后能正确回传到结算页

---

### 4.4 下单流程演示

**演示步骤：**

1. **进入结算页**
   - 在菜单页点击底部购物车图标
   - 或点击"去结算"按钮
2. **选择收货地址**
   - 点击地址区域
   - 选择或新增地址
3. **选择优惠券（可选）**
   - 点击"选择优惠券"
   - 查看可用优惠券列表
   - 选择一张优惠券
   - 查看价格变化（原价 → 优惠后价格）
4. **提交订单**
   - 确认订单信息（商品、数量、价格）
   - 点击"提交订单"
   - 订单创建成功，跳转到订单详情页

**验证点：**
- ✅ 地址选择功能正常
- ✅ 优惠券选择功能正常
- ✅ 价格计算正确（原价 - 优惠金额 = 实付金额）
- ✅ 订单创建成功，生成订单号

---

### 4.5 订单管理功能演示

**演示步骤：**

1. **查看订单列表**
   - 点击底部"订单"标签
   - 查看所有订单列表
   - 订单按状态分类（待支付、制作中、待取餐、已完成、已取消）
2. **查看订单详情**
   - 点击订单卡片
   - 查看订单详细信息：
     - 订单号
     - 商品列表
     - 收货地址
     - 订单状态
     - 取餐码（如果状态为"待取餐"）
3. **支付订单**
   - 在订单列表中找到"待支付"订单
   - 点击"立即支付"
   - 支付成功，订单状态变为"制作中"
4. **取消订单**
   - 在订单列表中找到"待支付"或"制作中"订单
   - 点击"取消订单"
   - 确认取消
   - 订单状态变为"已取消"

**验证点：**
- ✅ 订单列表正常显示
- ✅ 订单状态正确更新
- ✅ 支付功能正常（模拟支付）
- ✅ 取消订单功能正常
- ✅ 取餐码正确显示（状态为"待取餐"时）

---

### 4.6 AI 智能助手功能演示

**演示步骤：**

1. **进入 AI 助手**
   - 点击"我的" → "AI助手"
   - 或从菜单页进入
2. **与 AI 对话**
   - 输入问题："今天营业时间是什么？"
   - AI 回复营业时间信息
   - 输入："推荐一杯好喝的咖啡"
   - AI 推荐商品，并显示推荐商品卡片
3. **查看推荐商品**
   - 点击推荐商品卡片
   - 跳转到菜单页，定位到该商品
4. **测试个性化推荐**
   - 先收藏几个商品
   - 再下单几个商品
   - 询问 AI："根据我的喜好推荐"
   - AI 基于收藏和订单历史推荐

**验证点：**
- ✅ AI 对话功能正常
- ✅ 推荐商品卡片正常显示
- ✅ 点击推荐商品能正确跳转
- ✅ 个性化推荐基于用户数据（如果配置了 DeepSeek API Key）

---

### 4.7 优惠券功能演示

**演示步骤：**

1. **查看优惠券列表**
   - 点击"我的" → "优惠券"
   - 查看可用优惠券列表
2. **领取优惠券**
   - 点击"立即领取"
   - 领取成功，优惠券数量增加
3. **使用优惠券**
   - 在结算页选择优惠券
   - 查看价格变化
   - 提交订单时使用优惠券

**验证点：**
- ✅ 优惠券列表正常显示
- ✅ 领取功能正常
- ✅ 使用优惠券后价格正确计算

---

### 4.8 用户反馈功能演示

**演示步骤：**

1. **提交反馈**
   - 点击"我的" → "意见反馈"
   - 输入反馈内容
   - 上传图片（可选）
   - 提交
2. **查看反馈记录**
   - 在后台管理系统查看反馈列表
   - 处理反馈

**验证点：**
- ✅ 反馈提交成功
- ✅ 反馈内容正确保存

---

## 🖥️ 第五步：后台管理演示（10分钟）

### 5.1 访问后台管理

打开浏览器访问：http://localhost:8081/admin.html

### 5.2 数据统计展示

- 查看用户总数
- 查看商品总数
- 查看今日订单数
- 查看风险用户数

### 5.3 商品管理

访问：http://localhost:8081/product_manage.html

**功能：**
- 查看商品列表
- 添加新商品
- 编辑商品信息
- 上架/下架商品
- 删除商品

### 5.4 订单管理

访问：http://localhost:8081/store_order_manage.html

**功能：**
- 查看所有订单
- 筛选订单状态
- 查看订单详情
- 处理订单

### 5.5 AI 监控大屏

访问：http://localhost:8081/monitor.html

**功能：**
- 查看门店热力图
- 查看客流预测数据
- 查看实时统计数据

---

## 🔍 第六步：验证数据一致性（5分钟）

### 6.1 验证数据库数据

使用 Navicat/DBeaver 连接数据库，检查：

- ✅ `user` 表：有注册的用户数据
- ✅ `products` 表：有商品数据
- ✅ `orders` 表：有创建的订单数据
- ✅ `order_items` 表：有订单明细数据
- ✅ `user_address` 表：有地址数据
- ✅ `user_favorite` 表：有收藏数据
- ✅ `user_coupon` 表：有优惠券数据
- ✅ `coupon` 表：有优惠券模板数据

### 6.2 验证 Redis 缓存

```bash
# 进入 Redis 容器
docker exec -it zhizuo-redis redis-cli

# 查看所有 Key
KEYS *

# 查看验证码（如果存在）
GET sms:code:13800138000
```

### 6.3 验证 RabbitMQ 队列

访问：http://localhost:15673

登录：`guest` / `guest`

查看队列：
- `order.delay.queue` - 订单延迟队列
- 其他业务队列

---

## ⚠️ 常见问题排查

### 问题1：小程序无法连接后端

**症状：** 小程序显示"网络连接失败"

**解决方案：**
1. 检查 `seat_client/utils/request.js` 中的 `BASE_URL` 是否使用局域网 IP
2. 检查后端服务是否正常启动：`docker-compose ps`
3. 检查防火墙是否阻止了 8081 端口
4. 在浏览器中访问 `http://你的IP:8081/doc.html` 验证后端可访问

### 问题2：数据库连接失败

**症状：** 后端日志显示数据库连接错误

**解决方案：**
1. 检查 MySQL 容器是否正常运行：`docker-compose ps mysql`
2. 检查数据库名称是否正确：`zhizuo_db`
3. 检查环境变量配置：`docker-compose.yml` 中的数据库连接信息
4. 查看 MySQL 日志：`docker-compose logs mysql`

### 问题3：AI 功能无响应

**症状：** AI 对话没有回复

**解决方案：**
1. 检查 DeepSeek API Key 是否配置正确
2. 检查 AI 服务是否启动：`docker-compose ps ai-service`
3. 查看 AI 服务日志：`docker-compose logs ai-service`
4. 如果不配置 API Key，AI 将使用 Mock 模式，仍可演示

### 问题4：验证码发送失败

**症状：** 点击"获取验证码"没有反应

**解决方案：**
1. 检查 Redis 是否正常运行：`docker-compose ps redis`
2. 检查后端日志，查看是否有错误信息
3. 验证码存储在 Redis 中，Key 格式：`sms:code:手机号`

### 问题5：订单创建失败

**症状：** 提交订单时提示错误

**解决方案：**
1. 检查数据库表是否已初始化
2. 检查商品数据是否存在
3. 查看后端日志，定位具体错误
4. 检查订单表结构是否正确（执行 `alter_orders_table.sql`）

---

## 📊 演示总结

### ✅ 核心功能演示完成度

- [x] 用户登录注册（手机号+验证码）
- [x] 商品菜单浏览（分类、详情、收藏）
- [x] 购物车管理
- [x] 地址管理（新增、编辑、删除、默认）
- [x] 下单流程（地址选择、优惠券使用）
- [x] 订单管理（列表、详情、支付、取消）
- [x] AI 智能助手（对话、推荐）
- [x] 优惠券功能（领取、使用）
- [x] 用户反馈
- [x] 后台管理（数据统计、商品管理、订单管理）

### 🎯 演示亮点

1. **完整的 O2O 电商流程**：从浏览商品到下单支付的完整闭环
2. **AI 智能推荐**：基于用户数据的个性化推荐
3. **优惠券系统**：完整的优惠券领取和使用流程
4. **地址管理**：支持地图定位的地址选择
5. **订单状态流转**：待支付 → 制作中 → 待取餐 → 已完成
6. **后台管理系统**：数据统计、商品管理、订单管理

---

## 🎉 演示完成！

如果以上所有功能都能正常演示，说明项目已经**100%可以演示**！

如有任何问题，请参考"常见问题排查"部分，或查看项目日志进行调试。

