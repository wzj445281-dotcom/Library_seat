# 真机调试IP配置说明

## 📋 你的IP地址列表

根据 `ipconfig` 输出，你有以下IP地址：
- `192.168.48.1`
- `192.168.211.1`
- `192.168.78.196` ✅ **当前配置使用这个**
- `172.26.240.1`

## ✅ 当前配置

文件：`seat_client/utils/request.js`

```javascript
const BASE_URL = 'http://192.168.78.196:8080/api';
```

## 🔍 测试步骤

### 步骤1：在手机上测试网络连接

**在手机浏览器中访问：**
```
http://192.168.78.196:8080/doc.html
```

**结果判断：**
- ✅ **能打开** → IP正确，继续步骤2
- ❌ **打不开** → 尝试其他IP地址

### 步骤2：如果当前IP不行，尝试其他IP

#### 测试顺序：
1. `http://192.168.48.1:8080/doc.html`
2. `http://192.168.211.1:8080/doc.html`
3. `http://172.26.240.1:8080/doc.html`

#### 找到能访问的IP后，修改配置：

文件：`seat_client/utils/request.js`

```javascript
// 例如找到 192.168.48.1 能访问，就改成：
const BASE_URL = 'http://192.168.48.1:8080/api';
```

### 步骤3：清除缓存并重新编译

1. 在微信开发者工具中点击"编译"旁边的下拉箭头
2. 选择"清除缓存"
3. 重新编译

### 步骤4：真机调试测试

1. 点击"预览"按钮
2. 用微信扫描二维码
3. 在手机上测试点餐功能

## 🔧 如果所有IP都打不开

### 可能原因：
1. **防火墙阻止** - 需要允许8080端口
2. **手机和电脑不在同一WiFi** - 必须同一WiFi
3. **路由器设置** - 可能阻止了设备间通信

### 解决方法：

#### 方法1：关闭防火墙测试（临时）
1. Windows设置 → 防火墙
2. 临时关闭防火墙
3. 测试是否能访问

#### 方法2：允许8080端口
1. Windows设置 → 防火墙 → 高级设置
2. 入站规则 → 新建规则
3. 端口 → TCP → 8080
4. 允许连接

#### 方法3：检查路由器设置
- 确保"AP隔离"功能已关闭
- 确保设备间可以互相访问

## 📝 快速验证命令

### 在电脑上测试（确认后端正常）
```bash
# 浏览器访问
http://localhost:8080/doc.html
```

### 在手机上测试（确认网络连接）
```bash
# 手机浏览器访问
http://192.168.78.196:8080/doc.html
```

### 在微信小程序中测试（确认功能）
1. 打开小程序
2. 进入"点餐"页面
3. 查看是否能加载商品列表

## 🎯 推荐操作流程

1. **先在手机浏览器测试** `http://192.168.78.196:8080/doc.html`
2. **如果能打开** → 清除缓存，重新编译，真机调试
3. **如果打不开** → 尝试其他IP，找到能访问的
4. **如果都不行** → 检查防火墙和网络设置

## ⚠️ 重要提示

1. **手机和电脑必须在同一WiFi**
2. **不能使用手机热点**（手机无法访问电脑IP）
3. **每次修改IP后，必须清除缓存并重新编译**
4. **防火墙可能阻止连接**，需要允许8080端口

## 📞 下一步

1. 在手机浏览器测试 `http://192.168.78.196:8080/doc.html`
2. 告诉我结果（能打开还是打不开）
3. 如果能打开，清除缓存并重新编译
4. 如果打不开，尝试其他IP地址

