# 确认IP地址并更新配置

## ✅ 后端服务状态

根据 `netstat` 输出，后端服务正在运行：
```
TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING       45408
```

## 🔍 下一步：确认你的IP地址

### 方法1：查看所有IP地址
运行命令：
```bash
ipconfig | findstr /i "IPv4"
```

### 方法2：在手机上测试

1. **确保手机和电脑连接同一个WiFi**
2. **在手机浏览器中依次测试以下地址：**
   - `http://192.168.48.1:8080/doc.html`
   - `http://192.168.211.1:8080/doc.html`
   - `http://172.20.10.2:8080/doc.html`
   - `http://172.26.240.1:8080/doc.html`

3. **哪个能打开API文档，就用哪个IP**

## 🔧 更新前端配置

### 步骤1：修改 BASE_URL

文件：`seat_client/utils/request.js`

**找到这一行：**
```javascript
const BASE_URL = 'http://192.168.78.196:8080/api';
```

**修改为你的实际IP，例如：**
```javascript
const BASE_URL = 'http://192.168.48.1:8080/api';  // 替换为你的实际IP
```

### 步骤2：清除缓存并重新编译

1. 在微信开发者工具中点击"编译"旁边的下拉箭头
2. 选择"清除缓存"
3. 重新编译

### 步骤3：真机调试

1. 点击"预览"按钮
2. 用微信扫描二维码
3. 在手机上测试功能

## 📝 如何判断哪个IP是正确的

### WiFi连接的IP通常是：
- `192.168.x.x` - 最常见
- `172.16.x.x` 到 `172.31.x.x` - 也可能
- `10.x.x.x` - 较少见

### 排除的IP：
- `127.0.0.1` - 本地回环，手机无法访问
- `169.254.x.x` - 自动配置失败，不能用
- `192.168.48.1` 或 `192.168.211.1` - 可能是虚拟网卡

### 推荐测试顺序：
1. 先测试 `192.168.48.1` 和 `192.168.211.1`
2. 如果都不行，测试 `172.20.10.2`
3. 如果还不行，查看 `ipconfig` 的完整输出，找到连接WiFi的网卡

## 🎯 快速验证

### 在电脑上测试（确认后端正常）
```
http://localhost:8080/doc.html
```
应该能看到API文档

### 在手机上测试（确认网络连接）
```
http://你的IP:8080/doc.html
```
应该能看到API文档

### 在微信小程序中测试（确认功能）
1. 打开小程序
2. 进入"点餐"页面
3. 应该能看到商品列表

## ⚠️ 如果手机无法访问

### 可能原因：
1. **防火墙阻止** - 需要允许8080端口
2. **IP地址不对** - 需要确认正确的IP
3. **不在同一WiFi** - 手机和电脑必须同一WiFi

### 解决方法：
1. **关闭防火墙测试**（临时）
2. **确认IP地址**（使用 `ipconfig` 查看）
3. **确保同一WiFi**（不能是手机热点）

## 📞 下一步

1. 运行 `ipconfig | findstr /i "IPv4"` 查看所有IP
2. 在手机上测试哪个IP能访问 `http://IP:8080/doc.html`
3. 修改 `request.js` 中的 `BASE_URL`
4. 清除缓存并重新编译
5. 真机调试测试

