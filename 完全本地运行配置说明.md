# 完全本地运行配置说明

## ✅ 已完成的修改

### 1. 所有请求工具已改为本地运行

**修改的文件：**
- ✅ `seat_client/utils/request.js` - 改为 `http://localhost:8080/api`
- ✅ `seat_client/utils/request-cached.js` - 改为 `http://localhost:8080/api`
- ✅ `seat_client/utils/request-fast.js` - 改为 `http://localhost:8080/api`
- ✅ `seat_client/utils/websocket.js` - 改为 `ws://localhost:8080`

### 2. 后端配置（已确认）

**文件：** `seat_server/src/main/resources/application.yml`

- ✅ 端口：8080
- ✅ 数据库：`localhost:3306/zhizuo`
- ✅ 已配置为本地MySQL

## 🚀 运行架构

```
┌─────────────────┐
│  微信开发者工具   │
│  (前端小程序)    │
└────────┬────────┘
         │ localhost:8080
         │ (通过工具代理)
         ▼
┌─────────────────┐
│  SpringBoot     │
│  (后端服务)      │
│  端口: 8080     │
└────────┬────────┘
         │ localhost:3306
         ▼
┌─────────────────┐
│     MySQL       │
│  (数据库)       │
│  端口: 3306     │
└─────────────────┘
```

**特点：**
- ✅ 完全本地运行
- ✅ 无需外部网络
- ✅ 无需配置IP地址
- ✅ 通过微信开发者工具代理访问

## 📝 配置步骤

### 步骤1：启动MySQL数据库

确保MySQL服务正在运行：
```bash
# Windows (服务管理器)
# 或命令行
net start MySQL80

# 检查端口
netstat -ano | findstr :3306
```

### 步骤2：启动SpringBoot后端

```bash
cd seat_server
mvn spring-boot:run
```

**确认后端运行：**
- 浏览器访问：`http://localhost:8080/doc.html`
- 应该能看到API文档

### 步骤3：配置微信开发者工具

1. **打开微信开发者工具**
2. **点击右上角"详情"**
3. **切换到"本地设置"标签**
4. **必须勾选：**
   - ✅ **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**
   - ✅ **不校验安全域名（TLS版本）**

**重要：** 必须勾选这些选项，否则无法访问 localhost！

### 步骤4：启动前端

1. **在微信开发者工具中打开项目**
   - 项目路径：`seat_client`
2. **清除缓存**
   - 点击"编译"旁边的下拉箭头
   - 选择"清除缓存"
3. **重新编译**
   - 点击"编译"按钮

## 🎯 测试验证

### 1. 测试后端

**浏览器访问：**
```
http://localhost:8080/doc.html
```

**应该看到：** API文档页面

### 2. 测试前端

**在微信开发者工具中：**
1. 打开小程序
2. 进入"点餐"页面
3. 应该能看到商品列表

### 3. 测试数据库

**确认数据库连接：**
- 后端启动日志中应该看到数据库连接成功
- 如果有错误，检查MySQL是否运行

## ⚠️ 注意事项

### 1. 微信开发者工具必须配置

**必须勾选：**
- ✅ 不校验合法域名
- ✅ 不校验安全域名

**否则无法访问 localhost！**

### 2. 后端必须运行

- 后端必须在 `localhost:8080` 运行
- 如果端口被占用，修改 `application.yml` 中的端口

### 3. MySQL必须运行

- MySQL必须在 `localhost:3306` 运行
- 数据库名：`zhizuo`
- 用户名：`root`
- 密码：`000000`（或你配置的密码）

### 4. 真机调试

**注意：** 真机调试时，手机无法直接访问 `localhost`！

**解决方案：**
- 使用微信开发者工具的"预览"功能（会通过电脑代理）
- 或者临时改为局域网IP（仅真机调试时）

## 🔧 故障排查

### 问题1：无法访问 localhost

**原因：** 微信开发者工具未配置

**解决：**
1. 详情 → 本地设置
2. 勾选"不校验合法域名"
3. 清除缓存并重新编译

### 问题2：后端连接失败

**检查：**
1. 后端是否运行：`netstat -ano | findstr :8080`
2. 控制台是否有错误
3. 数据库是否运行

### 问题3：数据库连接失败

**检查：**
1. MySQL是否运行：`netstat -ano | findstr :3306`
2. 数据库名是否正确：`zhizuo`
3. 用户名密码是否正确

## 📊 优势

### 完全本地运行的优势：

1. ✅ **无需网络** - 不依赖外部网络连接
2. ✅ **速度快** - 本地连接延迟极低
3. ✅ **稳定** - 不受网络波动影响
4. ✅ **安全** - 数据不经过外部网络
5. ✅ **简单** - 无需配置IP地址

## 🎉 完成

**所有配置已完成！**

现在可以：
1. 启动MySQL
2. 启动SpringBoot后端
3. 在微信开发者工具中打开前端
4. 开始开发测试

**完全本地运行，无需任何网络连接！** 🚀

