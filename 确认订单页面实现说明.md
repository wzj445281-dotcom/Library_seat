# 确认订单页面实现说明

## 📋 核心数据结构 (data)

```javascript
data: {
  // 购物车数据
  cartItems: [],              // 购物车商品列表
  totalPrice: 0,             // 商品总价
  originalPrice: 0,          // 原价（优惠前）
  discountAmount: 0,         // 优惠金额
  finalPrice: 0,             // 实付金额（优惠后）
  totalCount: 0,             // 商品总数量
  
  // 门店和用餐方式
  storeInfo: {},             // 门店信息 {name, address, id}
  diningType: 'self',        // 'self': 自取, 'delivery': 外卖
  remark: '',                // 订单备注
  
  // 提交状态
  isSubmitting: false,       // 防止重复提交
  
  // 优惠券相关
  availableCoupons: [],      // 可用优惠券列表
  selectedCoupon: null,      // 选中的优惠券 {userCouponId, title, amount, minPoint}
  showCouponPicker: false,   // 是否显示优惠券选择器
  
  // 地址相关
  selectedAddress: null      // 选中的收货地址 {id, name, phone, province, city, district, detail, isDefault}
}
```

---

## 🔑 关键函数实现

### 1. `onLoad(options)` - 页面加载

```javascript
onLoad(options) {
  this.initData();
  // 初始化完成后加载优惠券和地址
  this.loadAvailableCoupons();
  if (this.data.diningType === 'delivery') {
    this.loadDefaultAddress();
  }
}
```

**功能**:
- 初始化购物车数据和门店信息
- 加载可用优惠券列表
- 如果是外卖模式，加载默认地址

---

### 2. `onShow()` - 页面显示

```javascript
onShow() {
  // 从地址选择页返回时，刷新选中的地址
  const address = wx.getStorageSync('selectedAddress');
  if (address) {
    this.setData({ selectedAddress: address });
    wx.removeStorageSync('selectedAddress');
  }
}
```

**功能**:
- 监听从地址列表页返回
- 更新选中的地址（通过 `wx.setStorageSync` 传递）

---

### 3. `selectAddress()` - 选择地址

```javascript
selectAddress() {
  if (this.data.diningType !== 'delivery') {
    return;
  }
  
  // 跳转到地址列表页（选择模式）
  wx.navigateTo({
    url: '/pages/address/list?select=true',
    success: () => {
      // 页面跳转成功
    },
    fail: (err) => {
      console.error('跳转地址列表失败', err);
      wx.showToast({ title: '跳转失败', icon: 'none' });
    }
  });
}
```

**功能**:
- 仅在"外卖"模式下可用
- 跳转到地址列表页（`select=true` 表示选择模式）
- 地址列表页选择后，通过 `wx.setStorageSync('selectedAddress', address)` 传递选中的地址

**地址传递机制**:
- 使用 `wx.setStorageSync` 存储选中的地址
- 在 `onShow()` 中读取并更新页面数据

---

### 4. `loadAvailableCoupons()` - 加载可用优惠券

```javascript
loadAvailableCoupons() {
  // 等待价格计算完成后再加载优惠券
  if (this.data.originalPrice <= 0) {
    // 如果价格还没计算，延迟加载
    setTimeout(() => {
      this.loadAvailableCoupons();
    }, 100);
    return;
  }
  
  const couponApi = require('../../api/coupon');
  couponApi.getAvailableCoupons(this.data.originalPrice)
    .then(res => {
      if (res.code === 200) {
        this.setData({ availableCoupons: res.data || [] });
      }
    })
    .catch(err => {
      console.error('加载优惠券失败', err);
      // 静默失败，不影响下单流程
    });
}
```

**功能**:
- 根据订单原价获取可用优惠券列表
- 调用 `/app/coupon/available` API，传递 `totalPrice` 参数
- 静默失败，不影响下单流程

---

### 5. `calculatePrice()` / `calcTotal()` - 计算价格

```javascript
calcTotal() {
  let total = 0;
  let count = 0;
  this.data.cartItems.forEach(item => {
    total += item.price * item.count;
    count += item.count;
  });
  
  // 计算优惠后价格
  let discountAmount = 0;
  let finalPrice = total;
  if (this.data.selectedCoupon) {
    discountAmount = parseFloat(this.data.selectedCoupon.amount) || 0;
    finalPrice = Math.max(0, total - discountAmount);
  }
  
  this.setData({
    totalPrice: total,
    originalPrice: total,
    discountAmount: discountAmount,
    finalPrice: finalPrice,
    totalCount: count
  });
  
  // 价格变化后，重新加载可用优惠券
  this.loadAvailableCoupons();
}
```

**功能**:
- 计算商品总价和总数量
- 如果有选中的优惠券，计算优惠金额和实付金额
- 更新页面数据
- 价格变化后重新加载可用优惠券

---

### 6. `selectCoupon(e)` - 选择优惠券

```javascript
selectCoupon(e) {
  const index = e.currentTarget.dataset.index;
  const coupon = this.data.availableCoupons[index];
  
  this.setData({
    selectedCoupon: coupon,
    showCouponPicker: false
  });
  
  // 重新计算价格
  this.calcTotal();
}
```

**功能**:
- 从优惠券列表中选择一个优惠券
- 关闭优惠券选择器
- 重新计算价格（自动应用优惠）

---

### 7. `submitOrder()` - 提交订单

```javascript
submitOrder() {
  if (this.data.isSubmitting) return;

  // 1. 基础校验
  if (this.data.totalCount === 0) {
    wx.showToast({ title: '请先选择商品', icon: 'none' });
    return;
  }

  // 2. 外卖模式必须选择地址
  if (this.data.diningType === 'delivery') {
    if (!this.data.selectedAddress) {
      wx.showModal({
        title: '提示',
        content: '外卖配送需要选择收货地址，是否去添加？',
        confirmText: '去添加',
        cancelText: '取消',
        success: (res) => {
          if (res.confirm) {
            this.selectAddress();
          }
        }
      });
      return;
    }
  }

  this.setData({ isSubmitting: true });
  wx.showLoading({ title: '正在下单...' });

  // 3. 构造后端需要的参数结构
  // 注意：后端期望 deliveryType 为数字：0=自取，1=外卖
  const deliveryType = this.data.diningType === 'delivery' ? 1 : 0;
  
  // 构造地址信息字符串（外卖模式）
  let addressInfo = null;
  if (this.data.diningType === 'delivery' && this.data.selectedAddress) {
    const addr = this.data.selectedAddress;
    addressInfo = `${addr.province || ''}${addr.city || ''}${addr.district || ''}${addr.detail || ''} ${addr.name || ''} ${addr.phone || ''}`.trim();
  }

  // 获取优惠券ID（如果有选中的优惠券）
  const userCouponId = this.data.selectedCoupon ? 
    (this.data.selectedCoupon.userCouponId || this.data.selectedCoupon.id) : null;

  const orderData = {
    items: this.data.cartItems.map(item => ({
      productId: item.productId || item.id,
      count: item.count || 1,
      spec: item.spec || ''
    })),
    deliveryType: deliveryType, // 0=自取, 1=外卖
    remark: this.data.remark || '',
    userCouponId: userCouponId,
    addressInfo: addressInfo
  };

  orderApi.createOrder(orderData)
      .then(res => {
        wx.hideLoading();
        if (res.code === 200) {
          // 下单成功，后端返回订单号
          const orderNo = res.data || res.data?.orderNo;
          this.handleOrderSuccess(orderNo);
        } else {
          // 业务失败
          this.handleOrderFail(res.message || '下单失败');
        }
      })
      .catch(err => {
        wx.hideLoading();
        console.error('下单异常', err);
        wx.showToast({
          title: err.message || '网络异常，请重试',
          icon: 'none',
          duration: 2000
        });
      })
      .finally(() => {
        this.setData({ isSubmitting: false });
      });
}
```

**功能**:
- 防止重复提交
- 基础校验：商品数量
- 外卖模式校验：必须选择地址
- 构造订单数据：
  - `items`: 商品列表
  - `deliveryType`: 0=自取, 1=外卖
  - `remark`: 备注
  - `userCouponId`: 优惠券ID（可选）
  - `addressInfo`: 地址信息字符串（外卖模式，可选）
- 调用创建订单API
- 处理成功/失败回调

**关键点**:
- `deliveryType` 必须是数字（0或1），不是字符串
- `addressInfo` 是拼接的字符串格式：`省市区详细地址 姓名 电话`
- `userCouponId` 从 `selectedCoupon.userCouponId` 或 `selectedCoupon.id` 获取

---

## 📡 后端API接口

### 创建订单接口

**路径**: `POST /api/app/store/order/create`

**请求参数**:
```json
{
  "items": [
    {
      "productId": 1,
      "count": 2,
      "spec": "标准糖/冰/大杯"
    }
  ],
  "deliveryType": 1,        // 0=自取, 1=外卖
  "remark": "不要辣",        // 可选
  "userCouponId": 123,      // 可选，优惠券ID
  "addressInfo": "广东省深圳市南山区科技园南区 张三 13800138000"  // 可选，外卖模式必需
}
```

**响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": "ORDER20250124123456789"  // 订单号
}
```

---

## 🎨 UI交互流程

### 1. 地址选择流程

1. 用户点击地址栏（外卖模式）
2. 跳转到 `/pages/address/list?select=true`
3. 用户选择地址
4. 地址列表页调用 `wx.setStorageSync('selectedAddress', address)`
5. 返回确认订单页
6. `onShow()` 读取并更新 `selectedAddress`

### 2. 优惠券选择流程

1. 页面加载时调用 `loadAvailableCoupons()` 获取可用优惠券
2. 用户点击"选择优惠券"
3. 显示优惠券选择弹窗
4. 用户选择优惠券
5. 自动重新计算价格（`calcTotal()`）
6. 显示优惠金额和实付金额

### 3. 提交订单流程

1. 用户点击"去支付"
2. 校验商品数量
3. 外卖模式校验地址
4. 构造订单数据
5. 调用创建订单API
6. 成功后清空购物车和选中数据
7. 跳转到订单详情页

---

## ✅ 功能完整性检查

- [x] 地址选择功能（外卖模式）
- [x] 地址验证（外卖模式必须选择地址）
- [x] 优惠券加载（根据订单金额）
- [x] 优惠券选择
- [x] 价格计算（原价、优惠金额、实付金额）
- [x] 提交订单（传递地址和优惠券信息）
- [x] 错误处理
- [x] 防重复提交
- [x] 数据校验

---

## 🔧 注意事项

1. **地址传递**: 使用 `wx.setStorageSync` 在页面间传递地址数据
2. **deliveryType**: 必须是数字类型（0或1），不是字符串
3. **地址格式**: `addressInfo` 是拼接的字符串，格式：`省市区详细地址 姓名 电话`
4. **优惠券ID**: 从 `userCouponId` 或 `id` 字段获取
5. **价格计算**: 选择优惠券后自动重新计算，无需手动调用

---

## 🎉 总结

确认订单页面已完整实现：
- ✅ 地址选择功能（支持外卖模式）
- ✅ 优惠券核销功能（自动计算价格）
- ✅ 提交订单功能（传递完整参数）
- ✅ 数据校验和错误处理
- ✅ 良好的用户体验

现在可以在结算页面完成完整的下单流程！

